# 衣搭库网页版技术方案文档

## 一、文档概述

### 1.1 文档目的
本文档详细描述「衣搭库网页版 智能穿搭推荐」的技术实现方案，与产品需求文档（PRD）一致，**本期以网页版（Web）为优先交付形态**。**MVP 版本**：用户上传照片后**手动填写品类、颜色、风格、季节等属性**，不接入 AI 衣物识别；**AI 衣物识别**规划在**后续版本**接入，本章及第二章中相关接口与适配层设计保留供届时实现。豆包AI开放接口用于**穿搭推荐能力**（MVP 可接入），通过接口适配层实现统一封装，涵盖前端预处理、接口集成、断点续传、推荐算法、数据存储、性能优化和维护机制等核心模块。iOS/Android 原生 App 不在本期范围内，待网页版稳定运营后再规划开发。

### 1.2 技术目标
- **MVP 版本**：上传后**手动分类**（用户选择品类/颜色/风格/季节），上传体验与推荐效果优先落地。
- **上传体验**：支持断点续传（建议 1MB/块），网络中断后恢复上传成功率≥99%，单张照片上传耗时≤3秒（4G 网络）
- **推荐效果**：衣橱≥10件时生成3-5套符合场合/天气/用户偏好的穿搭，7天内不重复推荐；衣橱<10件时提供通用搭配模板
- **隐私合规**：用户照片仅本地/加密云端存储，不上传公共服务器，支持一键删除所有数据
- **性能约束**：推荐生成 P95≤3秒
- **后续版本（AI 识别接入后）**：AI 识别准确率（上衣/裤子/裙子/鞋子≥95%，配饰≥80%；颜色≥90%；风格≥85%；季节≥90%；用户修正率≤10%）；识别单张照片耗时≤5秒（主流浏览器）

### 1.3 技术栈
- **前端（Web）**：现代浏览器（Chrome/Firefox/Safari/Edge 近期版本），推荐技术选型：React/Vue/Next.js 等 + TypeScript，支持响应式与 PWA 可选
- **AI服务**：豆包AI开放接口（**MVP 仅使用穿搭推荐能力**；**衣物识别能力**在后续版本接入）
- **后端**：Go语言（Gin/Echo框架）
- **接口适配层**：Go语言实现，统一API封装、异常重试、数据加密、结果解析
- **数据库**：MySQL（业务数据）、Redis（缓存）
- **存储**：七牛云OSS（文件存储）

---

## 二、豆包AI接口集成

**版本说明**：**MVP 版本**仅使用豆包AI的**穿搭推荐能力**；**衣物识别能力**（2.3.1、2.4 识别相关适配、2.5 识别流程、2.6 准确率监控）规划在**后续版本**接入，本节中相关接口与适配层设计保留供届时实现。MVP 下用户上传照片后在前端**手动选择品类、颜色、风格、季节**等属性并提交保存。

### 2.1 架构概述

**核心架构流程**：
```
衣搭库网页版（浏览器）
    ↓（前端预处理：图片压缩/匿名化/分块）
接口适配层（统一API封装/异常重试/数据加密）
    ↓（HTTPS加密传输）
豆包AI开放接口（衣物识别+穿搭推荐能力）
    ↓（返回结构化结果）
接口适配层（结果解析/格式转换/缓存）
    ↓
衣搭库网页版（结果展示/用户交互/本地缓存与云端同步）
```

**技术优势**：
- 无需自建AI模型，降低开发和运维成本
- 利用豆包AI成熟的识别能力，准确率高
- 统一的接口适配层，便于后续切换或扩展AI服务
- 前端预处理保证数据安全和传输效率，浏览器内完成压缩与匿名化

### 2.2 前端预处理（浏览器端）

#### 2.2.1 图片压缩
**压缩策略**（与 PRD 一致）：
- 目标大小：单张照片≤2MB（保证识别质量）
- 压缩质量：JPEG 质量 80–85%
- 分辨率限制：最大边≤2048px（保持清晰度）

**前端实现要点**：
- 使用 Canvas API 进行缩放与重绘，控制最大宽高与输出质量
- 使用 `canvas.toBlob('image/jpeg', quality)` 或 `canvas.toDataURL('image/jpeg', quality)` 生成压缩结果
- 大图先缩小再压缩，避免单次解码过大位图导致内存峰值过高

**Web 实现示例**（TypeScript/JavaScript）：
```typescript
/**
 * 浏览器端图片压缩，目标 ≤ maxSizeKB，最大边 ≤ maxDimension
 */
async function compressImage(
  file: File,
  maxSizeKB: number = 2000,
  maxDimension: number = 2048,
  initialQuality: number = 0.85
): Promise<Blob> {
  const img = await createImageBitmap(file);
  let width = img.width, height = img.height;
  if (width > maxDimension || height > maxDimension) {
    const scale = Math.min(maxDimension / width, maxDimension / height);
    width = Math.floor(width * scale);
    height = Math.floor(height * scale);
  }
  const canvas = new OffscreenCanvas(width, height);
  const ctx = canvas.getContext('2d')!;
  ctx.drawImage(img, 0, 0, width, height);
  img.close();

  let quality = initialQuality;
  let blob = await new Promise<Blob | null>((resolve) =>
    canvas.convertToBlob({ type: 'image/jpeg', quality }, (b) => resolve(b))
  );
  if (!blob) throw new Error('compress failed');

  while (blob.size > maxSizeKB * 1024 && quality > 0.5) {
    quality -= 0.05;
    blob = await new Promise<Blob | null>((resolve) =>
      canvas.convertToBlob({ type: 'image/jpeg', quality }, (b) => resolve(b))
    );
    if (!blob) break;
  }
  return blob;
}
```

#### 2.2.2 匿名化处理
**处理内容**（与 PRD 一致）：
1. **去除 EXIF 信息**：移除位置、时间、设备等元数据（上传前由前端重绘到 Canvas 再导出，自然剥离 EXIF）
2. **人脸模糊**（可选）：可依赖后端或第三方服务检测并模糊人脸区域；纯前端可选方案为简单人脸检测库（如 face-api.js）后对区域做模糊
3. **背景简化**（可选）：提取衣物主体区域，可由后端或 AI 服务处理

**前端实现要点**：
- 通过 Canvas 绘制后再导出 JPEG/PNG，即可去除 EXIF，无需单独解析 EXIF 再写回
- 若启用人脸模糊，可在压缩前对 ImageBitmap 或 Canvas 上指定区域做模糊（如使用 Canvas 的 `filter: blur()` 或像素处理）

**Web 去除 EXIF 示例**：
```typescript
// 经 Canvas 重绘后导出的 Blob 不包含原图 EXIF
async function stripExif(file: File): Promise<Blob> {
  const img = await createImageBitmap(file);
  const canvas = new OffscreenCanvas(img.width, img.height);
  canvas.getContext('2d')!.drawImage(img, 0, 0);
  img.close();
  return new Promise((resolve, reject) => {
    canvas.convertToBlob({ type: 'image/jpeg', quality: 0.9 }, (blob) =>
      blob ? resolve(blob) : reject(new Error('convert failed'))
    );
  });
}
```

#### 2.2.3 图片分块（断点续传）
**分块策略**（与 PRD 一致）：
- **建议 1MB/块**：将照片分割为多个数据块（1MB/块），便于断点续传与进度记录
- 上传进度实时保存（前端 IndexedDB 或后端记录已上传块），网络恢复后从断点处继续上传
- 分块后通过后端分块上传接口（如 Multipart Upload）统一上传

### 2.3 豆包AI接口规范

#### 2.3.1 衣物识别接口（后续版本接入）
**接口定义**（参考豆包AI文档，MVP 不调用）：
```python
POST https://api.doubao.com/v1/clothing/recognize
Headers:
{
    "Authorization": "Bearer {api_key}",
    "Content-Type": "application/json"
}
Request:
{
    "image": "base64_encoded_image",
    "options": {
        "category": true,      # 识别品类
        "color": true,         # 识别颜色
        "style": true,         # 识别风格
        "season": true         # 识别季节
    }
}
Response:
{
    "code": 0,
    "message": "success",
    "data": {
        "category": {
            "name": "上衣",
            "confidence": 0.96,
            "subcategory": "衬衫"
        },
        "colors": [
            {
                "name": "深蓝色",
                "rgb": [0, 0, 139],
                "proportion": 0.65
            },
            {
                "name": "白色",
                "rgb": [255, 255, 255],
                "proportion": 0.25
            }
        ],
        "styles": ["通勤", "正式"],
        "season": "四季通用"
    }
}
```

#### 2.3.2 穿搭推荐接口
**接口定义**：
```python
POST https://api.doubao.com/v1/outfit/recommend
Headers:
{
    "Authorization": "Bearer {api_key}",
    "Content-Type": "application/json"
}
Request:
{
    "wardrobe": [
        {
            "item_id": "item_001",
            "category": "上衣",
            "colors": ["深蓝色", "白色"],
            "styles": ["通勤", "正式"],
            "season": "四季通用"
        },
        # ... 更多衣物
    ],
    "occasion": "职场通勤",
    "weather": {
        "temperature": 25,
        "condition": "晴天"
    },
    "user_preferences": {
        "styles": ["通勤", "正式"],
        "colors": ["深色系", "中性色"]
    }
}
Response:
{
    "code": 0,
    "message": "success",
    "data": {
        "outfits": [
            {
                "outfit_id": "outfit_001",
                "items": ["item_001", "item_005", "item_012"],
                "score": 0.92,
                "highlights": "深蓝色衬衫搭配黑色西裤，正式且优雅",
                "suitable_occasions": ["职场通勤", "商务会议"]
            },
            # ... 更多推荐方案
        ]
    }
}
```

### 2.4 接口适配层设计

#### 2.4.1 统一API封装
**适配层职责**：
1. 统一接口调用格式
2. 异常处理和重试机制
3. 数据加密和签名
4. 结果解析和格式转换
5. 缓存管理

**架构设计**（Go语言实现）：
```go
package adapter

import (
    "context"
    "encoding/json"
    "fmt"
    "math"
    "time"
    "github.com/go-redis/redis/v8"
)

type DoubaoAIAdapter struct {
    apiKey      string
    apiSecret   string
    baseURL     string
    cache       *redis.Client
    retryConfig RetryConfig
    httpClient  *http.Client
    cacheConfig CacheConfig
}

type RetryConfig struct {
    MaxRetries   int
    BackoffFactor int
    Timeout      time.Duration
}

type CacheConfig struct {
    RecognitionTTL time.Duration // 识别结果缓存过期时间：1小时
    RecommendationTTL time.Duration // 推荐结果缓存过期时间：30分钟
    SeasonTransitionTTL time.Duration // 季节更替时缩短的缓存过期时间：15分钟
}

func NewDoubaoAIAdapter(apiKey, apiSecret string, cache *redis.Client) *DoubaoAIAdapter {
    return &DoubaoAIAdapter{
        apiKey:      apiKey,
        apiSecret:   apiSecret,
        baseURL:     "https://api.doubao.com/v1",
        cache:       cache,
        retryConfig: RetryConfig{MaxRetries: 3, BackoffFactor: 2, Timeout: 30 * time.Second},
        httpClient:  &http.Client{Timeout: 30 * time.Second},
        cacheConfig: CacheConfig{
            RecognitionTTL:     1 * time.Hour,      // 识别结果：1小时
            RecommendationTTL:   30 * time.Minute,  // 推荐结果：30分钟
            SeasonTransitionTTL: 15 * time.Minute,  // 季节更替时：15分钟
        },
    }
}

func (a *DoubaoAIAdapter) RecognizeClothing(ctx context.Context, imageData []byte, options map[string]bool) (map[string]interface{}, error) {
    // 1. 检查缓存
    cacheKey := a.generateCacheKey(imageData, options)
    cachedResult, err := a.cache.Get(ctx, cacheKey).Result()
    if err == nil {
        var result map[string]interface{}
        if err := json.Unmarshal([]byte(cachedResult), &result); err == nil {
            return result, nil
        }
    }
    
    // 2. 准备请求
    requestData := a.prepareRequest(imageData, options)
    
    // 3. 调用豆包AI接口（带重试）
    response, err := a.callAPIWithRetry(ctx, "/clothing/recognize", requestData)
    if err != nil {
        return nil, err
    }
    
    // 4. 解析和转换结果
    result := a.parseResponse(response)
    
    // 5. 缓存结果（使用配置的过期时间，季节更替时缩短）
    resultJSON, _ := json.Marshal(result)
    ttl := a.getRecognitionCacheTTL()
    a.cache.Set(ctx, cacheKey, resultJSON, ttl)
    
    return result, nil
}

func (a *DoubaoAIAdapter) RecommendOutfit(ctx context.Context, wardrobe []map[string]interface{}, occasion string, weather map[string]interface{}, preferences map[string]interface{}) (map[string]interface{}, error) {
    // 1. 检查缓存
    cacheKey := a.generateRecommendCacheKey(wardrobe, occasion, weather)
    cachedResult, err := a.cache.Get(ctx, cacheKey).Result()
    if err == nil {
        var result map[string]interface{}
        if err := json.Unmarshal([]byte(cachedResult), &result); err == nil {
            return result, nil
        }
    }
    
    // 2. 准备请求
    requestData := map[string]interface{}{
        "wardrobe":         wardrobe,
        "occasion":         occasion,
        "weather":          weather,
        "user_preferences": preferences,
    }
    
    // 3. 调用豆包AI接口
    response, err := a.callAPIWithRetry(ctx, "/outfit/recommend", requestData)
    if err != nil {
        return nil, err
    }
    
    // 4. 解析结果
    result := a.parseResponse(response)
    
    // 5. 缓存结果（使用配置的过期时间，季节更替时缩短）
    resultJSON, _ := json.Marshal(result)
    ttl := a.getRecommendationCacheTTL()
    a.cache.Set(ctx, cacheKey, resultJSON, ttl)
    
    return result, nil
}
```

#### 2.4.2 异常重试机制
**重试策略**（Go语言实现）：
```go
func (a *DoubaoAIAdapter) callAPIWithRetry(ctx context.Context, endpoint string, data map[string]interface{}) (map[string]interface{}, error) {
    var lastErr error
    
    for attempt := 0; attempt < a.retryConfig.MaxRetries; attempt++ {
        response, err := a.callAPI(ctx, endpoint, data)
        if err == nil {
            code, _ := response["code"].(float64)
            if code == 0 {
                return response, nil
            }
            
            // 可重试的错误码
            if code == 429 || code == 500 || code == 502 || code == 503 {
                lastErr = fmt.Errorf("retryable error: %v", response["message"])
                if attempt < a.retryConfig.MaxRetries-1 {
                    waitTime := time.Duration(math.Pow(float64(a.retryConfig.BackoffFactor), float64(attempt))) * time.Second
                    time.Sleep(waitTime)
                    continue
                }
            } else {
                // 不可重试的错误
                return nil, fmt.Errorf("non-retryable error: %v", response["message"])
            }
        } else {
            // 网络错误，可重试
            lastErr = err
            if attempt < a.retryConfig.MaxRetries-1 {
                waitTime := time.Duration(math.Pow(float64(a.retryConfig.BackoffFactor), float64(attempt))) * time.Second
                time.Sleep(waitTime)
                continue
            }
        }
    }
    
    return nil, fmt.Errorf("API调用失败，已重试%d次: %v", a.retryConfig.MaxRetries, lastErr)
}

func (a *DoubaoAIAdapter) callAPI(ctx context.Context, endpoint string, data map[string]interface{}) (map[string]interface{}, error) {
    url := a.baseURL + endpoint
    
    jsonData, err := json.Marshal(data)
    if err != nil {
        return nil, err
    }
    
    req, err := http.NewRequestWithContext(ctx, "POST", url, bytes.NewBuffer(jsonData))
    if err != nil {
        return nil, err
    }
    
    // 设置请求头
    req.Header.Set("Content-Type", "application/json")
    req.Header.Set("Authorization", "Bearer "+a.apiKey)
    
    // 签名（如需要）
    signature := a.signRequest(data)
    req.Header.Set("X-Signature", signature)
    
    resp, err := a.httpClient.Do(req)
    if err != nil {
        return nil, err
    }
    defer resp.Body.Close()
    
    var result map[string]interface{}
    if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
        return nil, err
    }
    
    return result, nil
}
```

#### 2.4.3 数据加密
**加密方案**（Go语言实现）：
```go
import (
    "crypto/hmac"
    "crypto/sha256"
    "encoding/hex"
    "sort"
    "strings"
    "time"
)

func (a *DoubaoAIAdapter) signRequest(data map[string]interface{}) string {
    // 1. 排序参数
    keys := make([]string, 0, len(data))
    for k := range data {
        keys = append(keys, k)
    }
    sort.Strings(keys)
    
    // 2. 拼接参数字符串
    var params []string
    for _, k := range keys {
        v := data[k]
        params = append(params, fmt.Sprintf("%s=%v", k, v))
    }
    paramString := strings.Join(params, "&")
    
    // 3. 拼接签名字符串
    timestamp := time.Now().Unix()
    signString := fmt.Sprintf("%s%d%s", a.apiKey, timestamp, paramString)
    
    // 4. HMAC-SHA256签名
    h := hmac.New(sha256.New, []byte(a.apiSecret))
    h.Write([]byte(signString))
    signature := hex.EncodeToString(h.Sum(nil))
    
    return signature
}

func (a *DoubaoAIAdapter) encryptSensitiveData(data map[string]interface{}) map[string]interface{} {
    // 使用AES加密敏感数据
    encryptedData := make(map[string]interface{})
    sensitiveFields := []string{"user_id", "device_id"}
    
    for key, value := range data {
        if contains(sensitiveFields, key) {
            // 使用AES加密
            encrypted, err := aesEncrypt(fmt.Sprintf("%v", value), a.encryptionKey)
            if err == nil {
                encryptedData[key] = encrypted
            }
        } else {
            encryptedData[key] = value
        }
    }
    
    return encryptedData
}
```

#### 2.4.4 结果解析与格式转换
**格式转换**（Go语言实现）：
```go
func (a *DoubaoAIAdapter) parseResponse(response map[string]interface{}) map[string]interface{} {
    code, _ := response["code"].(float64)
    if code != 0 {
        message, _ := response["message"].(string)
        return map[string]interface{}{
            "error": fmt.Sprintf("API返回错误: %s", message),
        }
    }
    
    data, ok := response["data"].(map[string]interface{})
    if !ok {
        return response
    }
    
    // 转换为内部统一格式
    if category, exists := data["category"]; exists {
        categoryMap := category.(map[string]interface{})
        colors := data["colors"].([]interface{})
        
        result := map[string]interface{}{
            "category": map[string]interface{}{
                "name":        categoryMap["name"],
                "confidence":  categoryMap["confidence"],
                "subcategory": categoryMap["subcategory"],
            },
            "styles": data["styles"],
            "season": data["season"],
        }
        
        // 转换颜色数组
        colorList := make([]map[string]interface{}, 0)
        for _, c := range colors {
            colorMap := c.(map[string]interface{})
            colorList = append(colorList, map[string]interface{}{
                "name":       colorMap["name"],
                "rgb":        colorMap["rgb"],
                "proportion": colorMap["proportion"],
            })
        }
        result["colors"] = colorList
        
        return result
    }
    
    if outfits, exists := data["outfits"]; exists {
        outfitList := outfits.([]interface{})
        result := make([]map[string]interface{}, 0)
        
        for _, o := range outfitList {
            outfitMap := o.(map[string]interface{})
            result = append(result, map[string]interface{}{
                "outfit_id":          outfitMap["outfit_id"],
                "item_ids":            outfitMap["items"],
                "score":               outfitMap["score"],
                "highlights":          outfitMap["highlights"],
                "suitable_occasions":  outfitMap["suitable_occasions"],
            })
        }
        
        return map[string]interface{}{
            "outfits": result,
        }
    }
    
    return data
}
```

#### 2.4.5 缓存管理
**缓存策略**：
- **识别结果缓存**：1小时过期（季节更替时缩短为15分钟）
- **推荐结果缓存**：30分钟过期（季节更替时缩短为15分钟）
- **用户修正后**：立即删除对应的识别结果缓存和所有推荐缓存
- **季节更替期**：3-4月（春夏更替）、9-10月（秋冬更替）时自动缩短缓存时间

**实现**（Go语言）：
```go
import (
    "crypto/md5"
    "encoding/hex"
    "encoding/json"
)

func (a *DoubaoAIAdapter) generateCacheKey(imageData []byte, options map[string]bool) string {
    // 生成图片哈希
    imageHash := md5.Sum(imageData)
    imageHashStr := hex.EncodeToString(imageHash[:])
    
    // 生成选项哈希
    optionsJSON, _ := json.Marshal(options)
    optionsHash := md5.Sum(optionsJSON)
    optionsHashStr := hex.EncodeToString(optionsHash[:])
    
    return fmt.Sprintf("recognize:%s:%s", imageHashStr, optionsHashStr)
}

func (a *DoubaoAIAdapter) generateRecommendCacheKey(wardrobe []map[string]interface{}, occasion string, weather map[string]interface{}) string {
    // 生成衣橱哈希
    wardrobeJSON, _ := json.Marshal(wardrobe)
    wardrobeHash := md5.Sum(wardrobeJSON)
    wardrobeHashStr := hex.EncodeToString(wardrobeHash[:])
    
    // 生成天气哈希
    weatherJSON, _ := json.Marshal(weather)
    weatherHash := md5.Sum(weatherJSON)
    weatherHashStr := hex.EncodeToString(weatherHash[:])
    
    return fmt.Sprintf("recommend:%s:%s:%s", wardrobeHashStr, occasion, weatherHashStr)
}

// 获取识别结果缓存过期时间（季节更替时缩短）
func (a *DoubaoAIAdapter) getRecognitionCacheTTL() time.Duration {
    if a.isSeasonTransition() {
        return a.cacheConfig.SeasonTransitionTTL
    }
    return a.cacheConfig.RecognitionTTL
}

// 获取推荐结果缓存过期时间（季节更替时缩短）
func (a *DoubaoAIAdapter) getRecommendationCacheTTL() time.Duration {
    if a.isSeasonTransition() {
        return a.cacheConfig.SeasonTransitionTTL
    }
    return a.cacheConfig.RecommendationTTL
}

// 判断是否处于季节更替期（3-4月、9-10月）
func (a *DoubaoAIAdapter) isSeasonTransition() bool {
    now := time.Now()
    month := int(now.Month())
    // 3-4月（春夏更替）、9-10月（秋冬更替）
    return (month >= 3 && month <= 4) || (month >= 9 && month <= 10)
}

// 用户修正识别结果后，立即删除对应缓存
func (a *DoubaoAIAdapter) InvalidateRecognitionCache(ctx context.Context, imageData []byte, options map[string]bool) error {
    cacheKey := a.generateCacheKey(imageData, options)
    return a.cache.Del(ctx, cacheKey).Err()
}

// 用户修正识别结果后，删除所有推荐缓存（因为衣橱数据已变更，所有推荐需要重新生成）
func (a *DoubaoAIAdapter) InvalidateAllRecommendationCache(ctx context.Context) error {
    // 使用模式匹配删除所有推荐缓存
    pattern := "recommend:*"
    keys, err := a.cache.Keys(ctx, pattern).Result()
    if err != nil {
        return err
    }
    if len(keys) > 0 {
        return a.cache.Del(ctx, keys...).Err()
    }
    return nil
}

// 根据衣橱哈希删除特定推荐缓存（当知道具体衣橱数据时使用）
func (a *DoubaoAIAdapter) InvalidateRecommendationCacheByWardrobe(ctx context.Context, wardrobe []map[string]interface{}) error {
    // 生成衣橱哈希
    wardrobeJSON, _ := json.Marshal(wardrobe)
    wardrobeHash := md5.Sum(wardrobeJSON)
    wardrobeHashStr := hex.EncodeToString(wardrobeHash[:])
    
    // 删除所有包含该衣橱哈希的推荐缓存
    pattern := fmt.Sprintf("recommend:%s:*", wardrobeHashStr)
    keys, err := a.cache.Keys(ctx, pattern).Result()
    if err != nil {
        return err
    }
    if len(keys) > 0 {
        return a.cache.Del(ctx, keys...).Err()
    }
    return nil
}
```

**缓存管理使用示例**：
```go
// 用户修正识别结果时的处理
func (s *ClothingService) UpdateClothingRecognition(ctx context.Context, itemID string, imageData []byte, options map[string]bool, correctedData map[string]interface{}) error {
    // 1. 保存修正后的数据到数据库
    if err := s.db.UpdateClothing(itemID, correctedData); err != nil {
        return err
    }
    
    // 2. 立即删除对应的识别结果缓存
    if err := s.doubaoAdapter.InvalidateRecognitionCache(ctx, imageData, options); err != nil {
        // 记录日志，但不影响主流程
        log.Printf("删除识别缓存失败: %v", err)
    }
    
    // 3. 删除所有推荐缓存（因为衣橱数据已变更，所有推荐需要重新生成）
    // 注意：由于推荐缓存key基于wardrobeHash，衣橱变更后旧缓存已失效，但为保险起见仍删除所有推荐缓存
    if err := s.doubaoAdapter.InvalidateAllRecommendationCache(ctx); err != nil {
        // 记录日志，但不影响主流程
        log.Printf("删除推荐缓存失败: %v", err)
    }
    
    // 4. 记录用户修正反馈（用于准确率分析）
    if err := s.feedbackCollector.RecordCorrection(ctx, itemID, correctedData); err != nil {
        log.Printf("记录修正反馈失败: %v", err)
    }
    
    return nil
}
```

### 2.5 前端集成（网页版）

#### 2.5.1 调用流程
前端不直接调用豆包 AI，而是调用自有后端 API；后端再通过接口适配层调用豆包 AI。这样 API Key 与签名逻辑仅存在于服务端，保证安全。

**MVP 流程（上传 + 手动分类）**：用户选择照片 → 前端压缩/匿名化 → 前端上传至后端（或先分块上传至 OSS 再通知后端）→ **上传完成后进入「编辑单品」页，由用户手动选择品类、颜色、风格、季节等** → 提交保存至服务端。

**后续版本（接入 AI 识别后）**：用户选择照片 → 前端压缩/匿名化 → 前端上传至后端（或先分块上传至 OSS 再通知后端）→ 后端调用豆包 AI 识别 → 返回结构化结果给前端展示/编辑（用户可修正后保存）。

#### 2.5.2 前端调用示例（TypeScript/JavaScript）
**MVP**：上传完成后前端展示「编辑单品」表单，用户选择品类、颜色、风格、季节等，调用 `POST /api/v1/wardrobe/items` 保存；无识别接口调用。

**后续版本（AI 识别接入后）** 可沿用以下识别调用示例：
```typescript
async function recognizeClothing(file: File): Promise<RecognitionResult> {
  // 1. 前端预处理：压缩、去除 EXIF
  const blob = await compressImage(file);
  const stripped = await stripExif(new File([blob], file.name, { type: blob.type }));

  // 2. 上传至后端（或分块上传后传 file_id）
  const formData = new FormData();
  formData.append('image', stripped);

  const res = await fetch('/api/v1/clothing/recognize', {
    method: 'POST',
    headers: { 'Authorization': `Bearer ${getAccessToken()}` },
    body: formData,
  });
  if (!res.ok) throw new Error(await res.text());
  const data = await res.json();

  // 3. 后端已通过适配层调用豆包 AI 并解析结果，前端直接使用
  return data.data as RecognitionResult;
}
```
MVP 下前端仅负责：选择文件、压缩、匿名化、上传、**手动分类表单**、保存；推荐逻辑在服务端完成。后续版本接入识别后，前端增加展示/编辑识别结果。

### 2.6 准确率监控与优化

#### 2.6.1 准确率监控
**监控指标**：
- 各品类识别准确率（基于用户修正反馈）
- 用户修正率（目标≤10%）
- API调用成功率
- 平均响应时间

**监控实现**：
```python
class AccuracyMonitor:
    def track_recognition_result(self, item_id: str, api_result: dict, user_correction: dict = None):
        """追踪识别结果和用户修正"""
        if user_correction:
            # 记录用户修正
            self.log_correction(item_id, api_result, user_correction)
            
            # 计算准确率
            accuracy = self.calculate_accuracy(api_result, user_correction)
            self.update_accuracy_metrics(accuracy)
    
    def calculate_accuracy(self, api_result: dict, user_correction: dict) -> float:
        """计算准确率"""
        correct = 0
        total = 0
        
        # 品类准确率
        if api_result['category']['name'] == user_correction['category']:
            correct += 1
        total += 1
        
        # 颜色准确率（允许部分匹配）
        api_colors = set([c['name'] for c in api_result['colors']])
        user_colors = set(user_correction['colors'])
        color_match = len(api_colors & user_colors) / max(len(api_colors), len(user_colors))
        correct += color_match
        total += 1
        
        return correct / total
```

#### 2.6.2 优化策略
**1. 参数调优**：
- 根据准确率反馈调整识别选项
- 针对低准确率品类，增加识别选项或后处理

**2. 结果后处理**：
```python
def post_process_result(result: dict, user_preferences: dict) -> dict:
    """结果后处理优化"""
    # 1. 置信度过滤
    if result['category']['confidence'] < 0.8:
        # 低置信度：提示用户手动选择
        result['needs_manual_review'] = True
    
    # 2. 基于用户偏好的调整
    if result['category']['name'] in user_preferences.get('preferred_categories', []):
        result['category']['confidence'] *= 1.1  # 提升偏好品类的置信度
    
    return result
```

**3. 用户反馈闭环**：
- 收集用户修正数据
- 定期分析修正模式
- 反馈给豆包AI团队（如支持）或调整后处理策略

---

## 三、图片上传断点续传实现

### 3.1 分块上传机制（与 PRD 一致）

#### 3.1.1 文件分块策略
**分块大小**（与 PRD 一致）：
- **建议 1MB/块**：将照片分割为多个数据块（1MB/块），记录每个数据块的上传状态
- 上传进度实时保存（前端 IndexedDB 或后端记录已上传块），网络恢复后从断点处继续上传
- 最小块：256KB（避免过多请求）；可根据网络状况动态调整块大小

**分块算法**：
```python
class ChunkUploader:
    def __init__(self, file_path, chunk_size=1024*1024):
        self.file_path = file_path
        self.chunk_size = chunk_size
        self.file_size = os.path.getsize(file_path)
        self.total_chunks = (self.file_size + chunk_size - 1) // chunk_size
        self.chunks = []
    
    def split_file(self):
        """将文件分割为多个块"""
        with open(self.file_path, 'rb') as f:
            for i in range(self.total_chunks):
                chunk_data = f.read(self.chunk_size)
                chunk_hash = hashlib.md5(chunk_data).hexdigest()
                self.chunks.append({
                    'index': i,
                    'data': chunk_data,
                    'hash': chunk_hash,
                    'size': len(chunk_data)
                })
        return self.chunks
```

#### 3.1.2 服务端分块上传接口
**初始化上传**：
```python
POST /api/v1/upload/init
Request:
{
    "file_name": "clothing_001.jpg",
    "file_size": 5242880,
    "file_hash": "md5_hash",
    "total_chunks": 5
}
Response:
{
    "upload_id": "upload_uuid",
    "chunk_size": 1048576,
    "expires_at": "2024-01-01T12:00:00Z"
}
```

**上传分块**：
```python
POST /api/v1/upload/chunk
Request:
{
    "upload_id": "upload_uuid",
    "chunk_index": 0,
    "chunk_hash": "chunk_md5",
    "chunk_data": "base64_encoded_chunk"
}
Response:
{
    "status": "success",
    "uploaded_chunks": [0, 1, 2],  # 已上传的块索引
    "progress": 0.6
}
```

**完成上传**：
```python
POST /api/v1/upload/complete
Request:
{
    "upload_id": "upload_uuid",
    "file_hash": "md5_hash"
}
Response:
{
    "file_url": "https://storage.example.com/files/xxx.jpg",
    "file_id": "file_uuid"
}
```

### 3.2 进度记录与状态管理

#### 3.2.1 本地数据库设计
**上传任务表（upload_tasks）**：
```sql
CREATE TABLE upload_tasks (
    task_id TEXT PRIMARY KEY,
    file_path TEXT NOT NULL,
    file_name TEXT NOT NULL,
    file_size INTEGER NOT NULL,
    file_hash TEXT NOT NULL,
    total_chunks INTEGER NOT NULL,
    uploaded_chunks TEXT,  -- JSON数组：[0, 1, 2]
    upload_id TEXT,        -- 服务端返回的upload_id
    status TEXT,           -- pending/uploading/completed/failed
    created_at INTEGER,
    updated_at INTEGER
);

CREATE TABLE upload_chunks (
    task_id TEXT,
    chunk_index INTEGER,
    chunk_hash TEXT,
    status TEXT,           -- pending/uploading/completed/failed
    retry_count INTEGER DEFAULT 0,
    PRIMARY KEY (task_id, chunk_index),
    FOREIGN KEY (task_id) REFERENCES upload_tasks(task_id)
);
```

#### 3.2.2 进度记录实现（网页版）
**前端**：使用 IndexedDB 或 LocalStorage 持久化上传任务与已上传块索引，页面刷新或网络恢复后仍可恢复进度。也可将进度同步到后端（由后端记录 `upload_id` 与已上传块列表）。

**Web 示例（IndexedDB 存本地进度）**：
```typescript
const DB_NAME = 'ydk_upload';
const STORE = 'upload_tasks';

async function saveProgress(taskId: string, uploadedChunks: number[], totalChunks: number) {
  const db = await openDB(DB_NAME, 1);
  await db.put(STORE, { taskId, uploadedChunks, totalChunks, updatedAt: Date.now() });
}

async function getProgress(taskId: string): Promise<{ uploaded: number[]; total: number }> {
  const db = await openDB(DB_NAME, 1);
  const row = await db.get(STORE, taskId);
  return row ? { uploaded: row.uploadedChunks || [], total: row.totalChunks || 0 } : { uploaded: [], total: 0 };
}
```
**后端**：若采用服务端记录进度，则在上传分块接口中更新 `upload_tasks.uploaded_chunks`，断点续传时根据 `upload_id` 查询已上传块，仅上传未完成块。

### 3.3 网络监听与自动恢复（网页版）

#### 3.3.1 网络状态监听
**前端实现**：使用 `navigator.onLine` 与 `window` 的 `online` / `offline` 事件检测网络恢复，与 PRD 一致：网络恢复后自动触发断点续传，并可选弹出「正在恢复上传」提示（显示待恢复照片数量与进度）。

```typescript
function setupNetworkMonitor(onOnline: () => void) {
  window.addEventListener('online', () => {
    if (navigator.onLine) onOnline(); // 网络恢复，触发待上传任务恢复
  });
}

// 网络中断时：已选照片保存在本地队列，显示「等待上传」；网络恢复后自动恢复上传并可选弹窗：「正在恢复上传」「检测到 X 张照片未完成上传，正在自动恢复...」
```

#### 3.3.2 自动恢复机制
**恢复流程**：
```python
class UploadResumeManager:
    def __init__(self):
        self.network_monitor = NetworkMonitor()
        self.upload_queue = UploadQueue()
        self.network_monitor.on_network_available = self.resume_uploads
    
    def resume_uploads(self):
        """网络恢复后自动恢复上传"""
        pending_tasks = self.upload_queue.get_pending_tasks()
        for task in pending_tasks:
            self.resume_task(task.task_id)
    
    def resume_task(self, task_id):
        """恢复单个任务的上传"""
        task = self.upload_queue.get_task(task_id)
        uploaded_chunks = task.uploaded_chunks  # [0, 1, 2]
        total_chunks = task.total_chunks
        
        # 从未上传的块开始继续上传
        for chunk_index in range(total_chunks):
            if chunk_index not in uploaded_chunks:
                self.upload_chunk(task_id, chunk_index)
```

**用户提示**（与 PRD 一致）：
- 网络恢复时：自动弹出「正在恢复上传」弹窗，内容为「检测到 X 张照片未完成上传，正在自动恢复...」（X 为待恢复照片数量），可显示当前恢复进度（如「正在恢复第 2/5 张照片」），提供「取消」按钮关闭弹窗（上传在后台继续）。
- 弹窗关闭后，上传任务在后台继续，用户可正常使用其他功能。

**前端示例**：
```typescript
function showResumeDialog(pendingCount: number, currentIndex?: number, total?: number) {
  const message = total != null
    ? `检测到 ${pendingCount} 张照片未完成上传，正在恢复第 ${currentIndex}/${total} 张...`
    : `检测到 ${pendingCount} 张照片未完成上传，正在自动恢复...`;
  // 使用项目内 Modal/Toast 组件展示，带「取消」按钮，关闭后上传在后台继续
}
```

### 3.4 上传队列管理（与 PRD 一致）

#### 3.4.1 队列持久化与上传队列页面
**队列状态保存**：
- 任务状态：**等待上传**（pending）、**上传中**（uploading）、**恢复中**（resuming）、**上传失败**（failed）、已完成（completed）
- 已上传块索引：JSON 数组存储（前端 IndexedDB 或后端 upload_tasks 表）
- 重试次数：每个块独立记录

**上传队列页面**（PRD 要求）：
- 用户可在「上传队列」页面查看所有待上传照片（含中断的照片）
- 状态显示：**等待上传**、**上传中**（显示进度百分比）、**恢复中**（显示「正在恢复上传」）、**上传失败**（显示失败原因，如「网络错误」）
- 支持手动重试（单个或批量）、取消上传（已上传块保留，下次可继续）

#### 3.4.2 优先级管理
**优先级规则**：
1. 按创建时间排序（FIFO）
2. 失败任务：降低优先级，避免阻塞新任务
3. 用户手动重试：提升优先级

**实现**：
```python
class UploadQueue:
    def get_next_task(self):
        """获取下一个待上传任务"""
        tasks = self.db.query("""
            SELECT * FROM upload_tasks 
            WHERE status IN ('pending', 'failed')
            ORDER BY 
                CASE WHEN status = 'failed' THEN 1 ELSE 0 END,
                created_at ASC
            LIMIT 1
        """)
        return tasks[0] if tasks else None
```

#### 3.4.3 并发控制
**并发策略**：
- 同时最多3个上传任务并发
- 每个任务内部：串行上传块（避免网络拥塞）
- 批量上传：9张照片，每张独立任务，最多3个并发

**实现**：
```swift
class UploadManager {
    private let maxConcurrent = 3
    private var activeTasks: [String: UploadTask] = [:]
    private let queue = DispatchQueue(label: "UploadQueue", attributes: .concurrent)
    
    func addTask(_ task: UploadTask) {
        queue.async { [weak self] in
            self?.processQueue()
        }
    }
    
    private func processQueue() {
        while activeTasks.count < maxConcurrent {
            guard let nextTask = uploadQueue.getNextTask() else { break }
            startUpload(nextTask)
        }
    }
    
    private func startUpload(_ task: UploadTask) {
        activeTasks[task.id] = task
        // 开始上传...
        task.completion = { [weak self] in
            self?.activeTasks.removeValue(forKey: task.id)
            self?.processQueue()  // 继续处理队列
        }
    }
}
```

#### 3.4.4 失败重试机制
**重试策略**：
- 最多重试3次
- 指数退避：第1次重试等待1秒，第2次2秒，第3次4秒
- 超过3次：标记为失败，用户可手动重试

**实现**：
```python
class ChunkUploader:
    def upload_chunk_with_retry(self, task_id, chunk_index, max_retries=3):
        for attempt in range(max_retries):
            try:
                result = self.upload_chunk(task_id, chunk_index)
                if result['status'] == 'success':
                    return result
            except NetworkError as e:
                if attempt < max_retries - 1:
                    wait_time = 2 ** attempt  # 指数退避
                    time.sleep(wait_time)
                else:
                    self.mark_chunk_failed(task_id, chunk_index)
                    raise
```

### 3.5 识别中断处理（后续版本，AI 识别上线后）

**MVP 版本**：无识别任务，上传完成后引导用户进入「编辑单品」页手动填写品类、颜色、风格、季节并保存。仅需上传相关提示：网络中断时显示「网络连接已断开，照片已保存，网络恢复后将自动上传」。

**后续版本（接入 AI 识别后）**：
**识别任务与上传任务独立管理**：
- 上传完成后自动触发识别任务；识别请求失败时自动加入重试队列，网络恢复后自动重试
- 支持用户手动「重新识别」（前端提供「重新识别」按钮，调用后端识别接口）
- **识别完成后立即保存**到服务端（及前端缓存），避免数据丢失

**提示机制**（PRD）：
- 识别过程中显示「AI 正在识别中...」；识别失败时友好提示并支持重新识别

---

## 四、穿搭推荐算法

### 4.1 推荐引擎架构

#### 4.1.1 豆包AI推荐 + 本地后处理融合框架
**架构设计**：
- **主要推荐源**：豆包AI开放接口（穿搭推荐能力）
- **本地后处理**：规则引擎过滤、去重、多样性保证、用户偏好调整

**融合策略**：
1. **豆包AI推荐**（主要）：调用豆包AI接口获取推荐方案
2. **本地规则引擎**（后处理）：基于穿搭规则进行过滤和排序
3. **用户行为数据**（优化）：基于历史行为调整推荐权重

#### 4.1.2 推荐流程
```python
class OutfitRecommender:
    def __init__(self):
        self.doubao_adapter = DoubaoAIAdapter()
        self.rule_engine = RuleEngine()
        self.user_behavior_tracker = UserBehaviorTracker()
    
    def recommend(self, user_id, occasion, weather, user_preferences):
        # 1. 获取用户衣橱（与 PRD 一致：仅「可用」衣物参与推荐，过滤 is_retired=1 与 is_deleted=1）
        wardrobe = self.get_user_wardrobe(user_id)  # 后端查询时 WHERE is_retired=0 AND is_deleted=0
        
        # 2. 判断用户状态（冷启动/标准）
        if len(wardrobe) < 10:
            return self.cold_start_recommend(occasion, weather, user_preferences)
        
        # 3. 调用豆包AI推荐接口
        doubao_results = self.doubao_adapter.recommend_outfit(
            wardrobe=wardrobe,
            occasion=occasion,
            weather=weather,
            preferences=user_preferences
        )
        
        # 4. 本地后处理
        # 4.1 规则引擎过滤（确保符合基本穿搭规则）
        filtered_outfits = self.rule_engine.filter(doubao_results['outfits'], occasion, weather)
        
        # 4.2 基于用户行为调整权重
        adjusted_outfits = self.adjust_by_user_behavior(user_id, filtered_outfits)
        
        # 4.3 去重和多样性保证
        final_outfits = self.deduplicate(adjusted_outfits, user_id)
        final_outfits = self.ensure_diversity(final_outfits)
        
        return final_outfits[:5]  # 返回Top 5
    
    def adjust_by_user_behavior(self, user_id, outfits):
        """基于用户行为调整推荐权重"""
        behavior_data = self.user_behavior_tracker.get_user_behavior(user_id)
        
        for outfit in outfits:
            # 如果用户之前收藏过类似搭配，提升权重
            if self.is_similar_to_favorites(outfit, behavior_data['favorites']):
                outfit['score'] *= 1.2
            
            # 如果用户之前不感兴趣，降低权重
            if self.is_similar_to_dislikes(outfit, behavior_data['dislikes']):
                outfit['score'] *= 0.8
        
        # 重新排序
        outfits.sort(key=lambda x: x['score'], reverse=True)
        return outfits
```

### 4.2 规则引擎设计

#### 4.2.1 规则库结构
**规则分类**：
1. **颜色搭配规则**
2. **风格搭配规则**
3. **场合适配规则**
4. **季节适配规则**

**规则存储**：
```sql
CREATE TABLE matching_rules (
    rule_id TEXT PRIMARY KEY,
    rule_type TEXT,  -- color/style/occasion/season
    condition JSON,  -- 条件（如：上衣颜色=蓝色）
    action JSON,     -- 动作（如：下装颜色=白色/黑色）
    weight FLOAT,    -- 规则权重
    enabled BOOLEAN
);
```

#### 4.2.2 规则匹配算法
```python
class RuleEngine:
    def __init__(self):
        self.rules = self.load_rules()
    
    def score(self, candidates, occasion, weather, user_preferences):
        scores = {}
        for item in candidates:
            score = 0.0
            # 1. 场合适配规则
            score += self.match_occasion_rule(item, occasion) * 0.3
            # 2. 天气适配规则
            score += self.match_weather_rule(item, weather) * 0.2
            # 3. 用户偏好规则
            score += self.match_preference_rule(item, user_preferences) * 0.3
            # 4. 基础搭配规则（颜色、风格）
            score += self.match_basic_rules(item) * 0.2
            scores[item.id] = score
        return scores
    
    def match_occasion_rule(self, item, occasion):
        """场合适配规则"""
        rules = self.rules.filter(type='occasion', condition={'occasion': occasion})
        score = 0.0
        for rule in rules:
            if self.check_condition(item, rule.condition):
                score += rule.weight
        return min(score, 1.0)  # 归一化到[0,1]
```

#### 4.2.3 具体规则示例
**颜色搭配规则**：
```python
color_rules = [
    {
        'condition': {'top_color': 'blue'},
        'action': {'bottom_color': ['white', 'black', 'navy']},
        'weight': 0.8
    },
    {
        'condition': {'top_color': 'red'},
        'action': {'bottom_color': ['black', 'white', 'denim']},
        'weight': 0.7
    },
    {
        'condition': {'top_color': 'neutral'},  # 中性色
        'action': {'bottom_color': 'any'},
        'weight': 0.9  # 中性色百搭
    }
]
```

**场合适配规则**：
```python
occasion_rules = {
    '职场通勤': {
        'style': ['正式', '通勤'],
        'color': ['深色系', '中性色'],
        'avoid': ['运动鞋', '破洞牛仔裤']
    },
    '约会': {
        'style': ['甜美', '优雅', '时尚'],
        'color': ['亮色', '柔和色系'],
        'prefer': ['裙子', '有设计感的上衣']
    }
}
```

### 4.3 冷启动策略

#### 4.3.1 冷启动判断
```python
def is_cold_start(wardrobe):
    """判断是否冷启动"""
    if len(wardrobe) < 10:
        return True
    # 检查关键品类
    categories = [item.category for item in wardrobe]
    required = ['上衣', '下装', '鞋子']
    if not all(cat in categories for cat in required):
        return True
    return False
```

#### 4.3.2 通用搭配模板
**模板库**：
```python
outfit_templates = [
    {
        'name': '经典通勤',
        'items': [
            {'category': '上衣', 'description': '白色/浅色衬衫'},
            {'category': '下装', 'description': '深色西裤/半身裙'},
            {'category': '鞋子', 'description': '黑色/棕色皮鞋'}
        ],
        'occasion': '职场通勤',
        'style': '正式'
    },
    {
        'name': '休闲日常',
        'items': [
            {'category': '上衣', 'description': '白T/条纹T'},
            {'category': '下装', 'description': '牛仔裤/休闲裤'},
            {'category': '鞋子', 'description': '小白鞋/运动鞋'}
        ],
        'occasion': '周末逛街',
        'style': '休闲'
    }
]
```

#### 4.3.3 冷启动推荐流程
```python
def cold_start_recommend(occasion, weather, user_preferences):
    # 1. 筛选匹配的模板
    templates = filter_templates(occasion, user_preferences)
    
    # 2. 匹配用户已有衣物（部分匹配）
    matched_outfits = []
    for template in templates:
        outfit = match_template_to_wardrobe(template, user_wardrobe)
        if outfit['match_rate'] > 0.5:  # 至少50%匹配
            matched_outfits.append(outfit)
    
    # 3. 生成补齐引导
    for outfit in matched_outfits:
        missing = detect_missing_categories(outfit)
        outfit['suggestion'] = f"建议上传{missing}以完善搭配"
    
    return matched_outfits
```

### 4.4 内容推荐算法

#### 4.4.1 特征提取
**衣物特征向量**：
```python
def extract_features(item):
    """提取衣物特征向量"""
    features = {
        'category': one_hot_encode(item.category),
        'colors': encode_colors(item.colors),  # 颜色向量
        'styles': one_hot_encode(item.styles),
        'season': one_hot_encode(item.season),
        'tags': encode_tags(item.tags)
    }
    # 拼接为统一向量
    feature_vector = concatenate([
        features['category'],
        features['colors'],
        features['styles'],
        features['season'],
        features['tags']
    ])
    return feature_vector
```

#### 4.4.2 相似度计算
```python
class ContentRecommender:
    def score(self, candidates, user_preferences):
        """基于内容相似度评分"""
        scores = {}
        # 提取用户偏好特征
        user_profile = self.build_user_profile(user_preferences)
        
        for item in candidates:
            item_features = extract_features(item)
            # 余弦相似度
            similarity = cosine_similarity(user_profile, item_features)
            scores[item.id] = similarity
        return scores
    
    def build_user_profile(self, preferences):
        """构建用户偏好画像"""
        profile = {
            'preferred_styles': preferences['styles'],
            'preferred_colors': preferences['colors'],
            'preferred_occasions': preferences['occasions']
        }
        return encode_profile(profile)
```

### 4.5 协同过滤算法

#### 4.5.1 用户行为数据
**行为类型**：
- 收藏推荐方案
- 应用推荐方案
- 不感兴趣
- "换一件"替换行为

**数据存储**：
```sql
CREATE TABLE user_behaviors (
    user_id TEXT,
    outfit_id TEXT,
    behavior_type TEXT,  -- favorite/apply/dislike/replace
    timestamp INTEGER,
    PRIMARY KEY (user_id, outfit_id, behavior_type)
);
```

#### 4.5.2 协同过滤实现
```python
class CollaborativeFilter:
    def __init__(self):
        self.user_item_matrix = self.build_matrix()
    
    def score(self, user_id, candidates):
        """基于协同过滤评分"""
        # 1. 找到相似用户
        similar_users = self.find_similar_users(user_id, k=10)
        
        # 2. 计算候选物品的预测评分
        scores = {}
        for item in candidates:
            score = 0.0
            for similar_user, similarity in similar_users:
                if item.id in similar_user['favorite_items']:
                    score += similarity * 1.0  # 相似用户喜欢的物品
            scores[item.id] = score
        return scores
    
    def find_similar_users(self, user_id, k=10):
        """找到K个最相似用户"""
        user_vector = self.user_item_matrix[user_id]
        similarities = []
        for other_user_id, other_vector in self.user_item_matrix.items():
            if other_user_id != user_id:
                similarity = cosine_similarity(user_vector, other_vector)
                similarities.append((other_user_id, similarity))
        similarities.sort(key=lambda x: x[1], reverse=True)
        return similarities[:k]
```

### 4.6 去重与多样性保证

#### 4.6.1 去重机制
**7天内不重复推荐**：
```python
def deduplicate(outfits, user_id):
    """去除7天内已推荐过的搭配"""
    recent_outfits = get_recent_outfits(user_id, days=7)
    recent_item_ids = set()
    for outfit in recent_outfits:
        recent_item_ids.update(outfit['item_ids'])
    
    filtered_outfits = []
    for outfit in outfits:
        outfit_item_ids = set(item['id'] for item in outfit['items'])
        # 如果搭配中所有单品都在最近推荐过，则跳过
        if not outfit_item_ids.issubset(recent_item_ids):
            filtered_outfits.append(outfit)
    
    return filtered_outfits
```

#### 4.6.2 多样性保证
**多样性策略**：
1. **风格多样性**：确保3-5套推荐涵盖不同风格
2. **颜色多样性**：避免所有推荐都是同一色系
3. **品类多样性**：确保不同单品组合

**实现**：
```python
def ensure_diversity(outfits, max_outfits=5):
    """保证推荐方案的多样性"""
    selected = []
    used_styles = set()
    used_colors = set()
    
    for outfit in outfits:
        # 检查风格多样性
        outfit_styles = set(outfit['styles'])
        if outfit_styles.issubset(used_styles) and len(selected) > 0:
            continue  # 风格重复，跳过
        
        # 检查颜色多样性
        outfit_colors = set(outfit['main_colors'])
        if outfit_colors.issubset(used_colors) and len(selected) > 2:
            continue  # 颜色重复，跳过
        
        selected.append(outfit)
        used_styles.update(outfit_styles)
        used_colors.update(outfit_colors)
        
        if len(selected) >= max_outfits:
            break
    
    return selected
```

---

## 五、数据存储与隐私保护

### 5.1 存储方案（网页版）

**网页版架构**：业务数据以**云端 MySQL**为主，用户通过账号登录后读写服务端数据；前端使用 **IndexedDB**（或 LocalStorage）做**离线缓存**（衣橱列表、收藏、推荐方案、上传队列进度），与 PRD 3.3 离线查看功能一致。多设备同步通过同一账号访问云端数据实现。

#### 5.1.1 数据库设计（后端 MySQL）
**MySQL数据库结构**：
```sql
-- 衣物表（支持 PRD「已淘汰」状态：is_retired=1 不参与推荐但保留记录）
CREATE TABLE clothing_items (
    item_id VARCHAR(64) PRIMARY KEY,
    user_id VARCHAR(64) NOT NULL,
    image_url VARCHAR(512) NOT NULL COMMENT '七牛云OSS URL',
    thumbnail_url VARCHAR(512) COMMENT '缩略图URL',
    category VARCHAR(32),
    colors JSON COMMENT '颜色数组',
    styles JSON COMMENT '风格数组',
    season VARCHAR(16),
    tags JSON COMMENT '标签数组',
    notes TEXT,
    is_deleted TINYINT(1) DEFAULT 0 COMMENT '软删除',
    is_retired TINYINT(1) DEFAULT 0 COMMENT '已淘汰：1=不参与推荐，保留记录',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_category (category),
    INDEX idx_is_deleted (is_deleted),
    INDEX idx_is_retired (is_retired)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='衣物表';

-- 穿搭推荐表
CREATE TABLE outfit_recommendations (
    outfit_id VARCHAR(64) PRIMARY KEY,
    user_id VARCHAR(64) NOT NULL,
    item_ids JSON COMMENT '单品ID数组',
    occasion VARCHAR(32),
    weather JSON COMMENT '天气信息',
    score DECIMAL(3,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='穿搭推荐表';

-- 用户偏好表
CREATE TABLE user_preferences (
    user_id VARCHAR(64) PRIMARY KEY,
    styles JSON COMMENT '风格偏好数组',
    occasions JSON COMMENT '场合偏好数组',
    colors JSON COMMENT '颜色偏好数组',
    height INT COMMENT '身高(cm)',
    weight INT COMMENT '体重(kg)',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户偏好表';

-- 上传任务表
CREATE TABLE upload_tasks (
    task_id VARCHAR(64) PRIMARY KEY,
    user_id VARCHAR(64) NOT NULL,
    file_name VARCHAR(255) NOT NULL,
    file_size BIGINT NOT NULL,
    file_hash VARCHAR(64) NOT NULL,
    total_chunks INT NOT NULL,
    uploaded_chunks JSON COMMENT '已上传块索引数组',
    upload_id VARCHAR(64),
    status VARCHAR(16) DEFAULT 'pending' COMMENT 'pending/uploading/completed/failed',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='上传任务表';
```

#### 5.1.2 文件存储（七牛云OSS）
**存储架构**：
- 所有图片文件存储在七牛云 OSS
- **网页版**：前端可缓存缩略图与衣橱数据到 IndexedDB，用于离线查看（PRD 3.3）
- 图片 URL 与业务数据存储在 MySQL 数据库中

**七牛云OSS配置**：
```go
package storage

import (
    "github.com/qiniu/go-sdk/v7/auth/qbox"
    "github.com/qiniu/go-sdk/v7/storage"
)

type QiniuOSS struct {
    accessKey string
    secretKey string
    bucket    string
    domain    string
    mac       *qbox.Mac
    cfg       *storage.Config
}

func NewQiniuOSS(accessKey, secretKey, bucket, domain string) *QiniuOSS {
    mac := qbox.NewMac(accessKey, secretKey)
    cfg := &storage.Config{
        Zone:          &storage.ZoneHuadong, // 华东区域
        UseHTTPS:      true,
        UseCdnDomains: true,
    }
    
    return &QiniuOSS{
        accessKey: accessKey,
        secretKey: secretKey,
        bucket:    bucket,
        domain:    domain,
        mac:       mac,
        cfg:       cfg,
    }
}

func (q *QiniuOSS) UploadFile(key string, data []byte) (string, error) {
    // 生成上传凭证
    putPolicy := storage.PutPolicy{
        Scope: q.bucket,
    }
    putPolicy.Expires = 3600 // 1小时有效期
    upToken := putPolicy.UploadToken(q.mac)
    
    // 上传文件
    formUploader := storage.NewFormUploader(q.cfg)
    ret := storage.PutRet{}
    err := formUploader.Put(nil, &ret, upToken, key, bytes.NewReader(data), int64(len(data)), nil)
    if err != nil {
        return "", err
    }
    
    // 返回文件URL
    return fmt.Sprintf("https://%s/%s", q.domain, ret.Key), nil
}

func (q *QiniuOSS) DeleteFile(key string) error {
    bucketManager := storage.NewBucketManager(q.mac, q.cfg)
    return bucketManager.Delete(q.bucket, key)
}
```

**Go语言后端实现**：
```go
package service

import (
    "context"
    "fmt"
    "time"
)

type FileService struct {
    qiniuOSS *storage.QiniuOSS
    db       *gorm.DB
}

func (s *FileService) UploadImage(ctx context.Context, userID string, imageData []byte, itemID string) (string, error) {
    // 1. 生成文件key（路径）
    key := fmt.Sprintf("users/%s/items/%s/%d.jpg", userID, itemID, time.Now().Unix())
    
    // 2. 上传到七牛云OSS
    url, err := s.qiniuOSS.UploadFile(key, imageData)
    if err != nil {
        return "", fmt.Errorf("上传失败: %v", err)
    }
    
    // 3. 生成缩略图并上传
    thumbnailData := generateThumbnail(imageData, 256, 256)
    thumbnailKey := fmt.Sprintf("users/%s/items/%s/thumbnail_%d.jpg", userID, itemID, time.Now().Unix())
    thumbnailURL, err := s.qiniuOSS.UploadFile(thumbnailKey, thumbnailData)
    if err != nil {
        // 缩略图上传失败不影响主图
        thumbnailURL = url
    }
    
    // 4. 更新数据库
    err = s.db.Model(&ClothingItem{}).
        Where("item_id = ?", itemID).
        Updates(map[string]interface{}{
            "image_url":     url,
            "thumbnail_url": thumbnailURL,
        }).Error
    
    return url, err
}
```

### 5.2 云端存储方案

#### 5.2.1 七牛云OSS存储架构
**存储策略**：
- **默认模式**：照片上传识别后，保留在七牛云OSS，支持多设备同步
- **仅识别模式**（可选）：识别后删除OSS文件，仅保留识别结果

**七牛云OSS配置**（Go语言）：
```go
package storage

import (
    "crypto/aes"
    "crypto/cipher"
    "crypto/rand"
    "io"
)

type EncryptedStorage struct {
    qiniuOSS *QiniuOSS
    aesKey   []byte
}

func (e *EncryptedStorage) UploadEncryptedImage(ctx context.Context, userID string, imageData []byte, itemID string) (string, error) {
    // 1. 加密图片数据
    encryptedData, err := e.encryptAES(imageData, e.getUserKey(userID))
    if err != nil {
        return "", err
    }
    
    // 2. 上传到七牛云OSS
    key := fmt.Sprintf("users/%s/items/%s/%d.enc", userID, itemID, time.Now().Unix())
    url, err := e.qiniuOSS.UploadFile(key, encryptedData)
    if err != nil {
        return "", err
    }
    
    return url, nil
}

func (e *EncryptedStorage) encryptAES(data []byte, key []byte) ([]byte, error) {
    block, err := aes.NewCipher(key)
    if err != nil {
        return nil, err
    }
    
    gcm, err := cipher.NewGCM(block)
    if err != nil {
        return nil, err
    }
    
    nonce := make([]byte, gcm.NonceSize())
    if _, err := io.ReadFull(rand.Reader, nonce); err != nil {
        return nil, err
    }
    
    ciphertext := gcm.Seal(nonce, nonce, data, nil)
    return ciphertext, nil
}
```

#### 5.2.2 匿名化处理
**EXIF信息去除**：
```python
from PIL import Image
from PIL.ExifTags import TAGS

def remove_exif(image_path):
    """去除EXIF信息"""
    image = Image.open(image_path)
    # 创建新图片（不包含EXIF）
    data = list(image.getdata())
    image_without_exif = Image.new(image.mode, image.size)
    image_without_exif.putdata(data)
    return image_without_exif
```

**人脸模糊处理**：
```python
import cv2

def blur_faces(image):
    """模糊人脸（如果检测到）"""
    face_cascade = cv2.CascadeClassifier('haarcascade_frontalface_default.xml')
    gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)
    faces = face_cascade.detectMultiScale(gray, 1.1, 4)
    
    for (x, y, w, h) in faces:
        # 模糊人脸区域
        image[y:y+h, x:x+w] = cv2.GaussianBlur(
            image[y:y+h, x:x+w],
            (99, 99),
            30
        )
    return image
```

### 5.3 多设备同步（网页版）

**网页版**：用户通过账号登录，衣橱与推荐等数据均在服务端；多设备即多浏览器/多终端使用同一账号，以**服务端数据为准**，无需端侧“多设备同步冲突”逻辑。前端仅做缓存与离线可用（见 6.3 离线缓存）。

#### 5.3.1 同步机制（后端版本控制，供未来扩展）
**版本控制**：
```sql
CREATE TABLE sync_versions (
    item_id TEXT PRIMARY KEY,
    version INTEGER DEFAULT 1,
    last_modified INTEGER,
    device_id TEXT,
    checksum TEXT  -- 数据校验和
);
```

**同步流程**：
```python
class SyncManager:
    def sync(self, user_id):
        """同步用户数据"""
        # 1. 获取本地修改
        local_changes = self.get_local_changes(user_id)
        
        # 2. 获取云端版本
        cloud_versions = self.get_cloud_versions(user_id)
        
        # 3. 检测冲突
        conflicts = self.detect_conflicts(local_changes, cloud_versions)
        
        # 4. 解决冲突
        for conflict in conflicts:
            resolved = self.resolve_conflict(conflict)
            self.apply_resolution(resolved)
        
        # 5. 上传本地修改
        self.upload_changes(local_changes)
        
        # 6. 下载云端修改
        cloud_changes = self.get_cloud_changes(user_id)
        self.apply_cloud_changes(cloud_changes)
```

#### 5.3.2 冲突处理策略
**冲突解决算法**：
```python
def resolve_conflict(local_item, cloud_item):
    """解决编辑冲突"""
    # 1. 最后写入获胜（默认策略）
    if cloud_item['last_modified'] > local_item['last_modified']:
        return cloud_item
    
    # 2. 字段级合并（对于可合并字段）
    merged = {
        'item_id': local_item['item_id'],
        'category': local_item['category'],  # 以本地为准
        'tags': list(set(local_item['tags'] + cloud_item['tags'])),  # 合并标签
        'notes': local_item['notes'] if local_item['notes'] else cloud_item['notes'],
        'last_modified': max(local_item['last_modified'], cloud_item['last_modified'])
    }
    
    # 3. 关键字段冲突：提示用户选择
    if local_item['category'] != cloud_item['category']:
        # 触发用户选择界面
        return prompt_user_choice(local_item, cloud_item)
    
    return merged
```

### 5.4 数据删除与GDPR合规

#### 5.4.1 一键删除实现
```python
class DataDeletion:
    def delete_all_user_data(self, user_id):
        """一键删除所有用户数据"""
        # 1. 删除本地数据
        self.delete_local_data(user_id)
        
        # 2. 删除云端数据
        self.delete_cloud_data(user_id)
        
        # 3. 删除识别结果
        self.delete_recognition_results(user_id)
        
        # 4. 匿名化行为数据（保留用于分析，但去除个人标识）
        self.anonymize_behavior_data(user_id)
        
        # 5. 确认删除完成
        self.confirm_deletion(user_id)
    
    def delete_cloud_data(self, user_id):
        """删除云端数据"""
        # 删除加密存储的文件
        s3_client.delete_objects(
            Bucket='yidaiku-encrypted',
            Prefix=f'users/{user_id}/'
        )
        
        # 删除数据库记录
        db.execute("DELETE FROM clothing_items WHERE user_id = ?", [user_id])
        db.execute("DELETE FROM outfit_recommendations WHERE user_id = ?", [user_id])
```

#### 5.4.2 数据恢复机制
**7天恢复窗口**：
```python
def soft_delete(item_id):
    """软删除（7天内可恢复）"""
    db.execute("""
        UPDATE clothing_items 
        SET is_deleted = 1, deleted_at = ?
        WHERE item_id = ?
    """, [current_timestamp(), item_id])
    
    # 7天后自动硬删除
    schedule_hard_delete(item_id, days=7)

def restore_item(item_id):
    """恢复已删除的衣物"""
    db.execute("""
        UPDATE clothing_items 
        SET is_deleted = 0, deleted_at = NULL
        WHERE item_id = ?
    """, [item_id])
```

---

## 六、性能优化

### 6.1 图片处理优化

#### 6.1.1 图片压缩
**压缩策略**：
- 上传前压缩：保持质量的同时减小体积
- 缩略图生成：列表显示缩略图（256×256），详情页显示原图

**iOS实现**：
```swift
extension UIImage {
    func compressed(maxSizeKB: Int = 500) -> Data? {
        var compression: CGFloat = 1.0
        var imageData = self.jpegData(compressionQuality: compression)
        
        while let data = imageData, data.count > maxSizeKB * 1024 && compression > 0.1 {
            compression -= 0.1
            imageData = self.jpegData(compressionQuality: compression)
        }
        return imageData
    }
    
    func thumbnail(size: CGSize = CGSize(width: 256, height: 256)) -> UIImage? {
        UIGraphicsBeginImageContextWithOptions(size, false, 0.0)
        defer { UIGraphicsEndImageContext() }
        self.draw(in: CGRect(origin: .zero, size: size))
        return UIGraphicsGetImageFromCurrentImageContext()
    }
}
```

**Android实现**：
```kotlin
fun Bitmap.compress(maxSizeKB: Int = 500): ByteArray? {
    var quality = 100
    var outputStream = ByteArrayOutputStream()
    this.compress(Bitmap.CompressFormat.JPEG, quality, outputStream)
    
    while (outputStream.toByteArray().size > maxSizeKB * 1024 && quality > 10) {
        outputStream.reset()
        quality -= 10
        this.compress(Bitmap.CompressFormat.JPEG, quality, outputStream)
    }
    return outputStream.toByteArray()
}
```

#### 6.1.2 图片懒加载
**列表懒加载**：
```swift
class LazyImageView: UIImageView {
    private var imageURL: URL?
    private var task: URLSessionDataTask?
    
    func loadImage(from url: URL) {
        imageURL = url
        // 取消之前的任务
        task?.cancel()
        
        // 先显示占位图
        self.image = UIImage(named: "placeholder")
        
        // 异步加载
        task = URLSession.shared.dataTask(with: url) { [weak self] data, response, error in
            guard let self = self, let data = data, error == nil else { return }
            DispatchQueue.main.async {
                if self.imageURL == url {  // 确保URL未改变
                    self.image = UIImage(data: data)
                }
            }
        }
        task?.resume()
    }
}
```

### 6.2 推荐生成优化

#### 6.2.1 结果缓存
**缓存策略**：
- 缓存最近7天的推荐结果
- 缓存键：用户ID + 场合 + 天气 + 衣橱版本号

**实现**（Go语言）：
```go
package cache

import (
    "context"
    "encoding/json"
    "fmt"
    "time"
    "github.com/go-redis/redis/v8"
)

type RecommendationCache struct {
    redisClient *redis.Client
    cacheTTL    time.Duration
}

func NewRecommendationCache(redisClient *redis.Client) *RecommendationCache {
    return &RecommendationCache{
        redisClient: redisClient,
        cacheTTL:    7 * 24 * time.Hour, // 7天
    }
}

func (c *RecommendationCache) GetCacheKey(userID, occasion, weather, wardrobeVersion string) string {
    return fmt.Sprintf("recommend:%s:%s:%s:%s", userID, occasion, weather, wardrobeVersion)
}

func (c *RecommendationCache) Get(ctx context.Context, userID, occasion, weather, wardrobeVersion string) ([]map[string]interface{}, error) {
    key := c.GetCacheKey(userID, occasion, weather, wardrobeVersion)
    cached, err := c.redisClient.Get(ctx, key).Result()
    if err == redis.Nil {
        return nil, nil // 缓存不存在
    }
    if err != nil {
        return nil, err
    }
    
    var recommendations []map[string]interface{}
    if err := json.Unmarshal([]byte(cached), &recommendations); err != nil {
        return nil, err
    }
    
    return recommendations, nil
}

func (c *RecommendationCache) Set(ctx context.Context, userID, occasion, weather, wardrobeVersion string, recommendations []map[string]interface{}) error {
    key := c.GetCacheKey(userID, occasion, weather, wardrobeVersion)
    data, err := json.Marshal(recommendations)
    if err != nil {
        return err
    }
    
    return c.redisClient.Set(ctx, key, data, c.cacheTTL).Err()
}
```

#### 6.2.2 异步生成
**后台任务队列**（Go语言）：
```go
package service

import (
    "context"
    "github.com/hibiken/asynq"
)

type RecommendationService struct {
    recommender *OutfitRecommender
    taskClient  *asynq.Client
}

func (s *RecommendationService) GenerateRecommendationsAsync(ctx context.Context, userID, occasion, weather string) error {
    // 创建异步任务
    task := asynq.NewTask("generate_recommendations", map[string]interface{}{
        "user_id":  userID,
        "occasion": occasion,
        "weather":  weather,
    })
    
    // 提交任务到队列
    _, err := s.taskClient.Enqueue(task, asynq.MaxRetry(3))
    return err
}

// 任务处理器
func (s *RecommendationService) HandleGenerateRecommendations(ctx context.Context, task *asynq.Task) error {
    // 解析任务参数
    params := task.Payload()
    userID := params["user_id"].(string)
    occasion := params["occasion"].(string)
    weather := params["weather"].(string)
    
    // 生成推荐
    recommendations, err := s.recommender.Recommend(ctx, userID, occasion, weather)
    if err != nil {
        return err
    }
    
    // 保存结果
    if err := s.saveRecommendations(ctx, userID, recommendations); err != nil {
        return err
    }
    
    // 发送通知
    s.sendNotification(ctx, userID, "推荐已生成")
    
    return nil
}
```

**客户端实现**：
```swift
func generateRecommendations(occasion: String, weather: String) {
    // 显示加载状态
    showLoading()
    
    // 异步请求
    API.shared.generateRecommendations(occasion: occasion, weather: weather) { [weak self] result in
        DispatchQueue.main.async {
            self?.hideLoading()
            switch result {
            case .success(let recommendations):
                self?.displayRecommendations(recommendations)
            case .failure(let error):
                self?.showError(error)
            }
        }
    }
}
```

### 6.3 离线缓存（网页版，与 PRD 3.3 一致）

#### 6.3.1 缓存策略
**缓存内容**：
- 衣橱数据（识别结果、缩略图 URL 或 Base64 小图）
- 已收藏的穿搭方案
- **推荐方案缓存**（PRD 3.3.1）：**普通用户**自动缓存最近 7 天；**会员用户**可自定义缓存时长（最多 30 天），支持手动保存推荐到收藏；超过缓存时长的推荐方案自动清理，已收藏不受影响

**缓存更新**：
- 网络可用时：请求接口后更新 IndexedDB 缓存
- 网络不可用时：从 IndexedDB 读取衣橱、收藏、缓存的推荐方案；不支持上传新照片、生成新推荐、同步云端（PRD 离线限制）

**前端实现要点**（IndexedDB）：
```typescript
const CACHE_DAYS_NORMAL = 7;
const CACHE_DAYS_MEMBER = 30;

async function cacheWardrobe(wardrobe: ClothingItem[]) {
  const db = await openDB('ydk_offline', 1);
  await db.put('wardrobe', { key: 'list', data: wardrobe, updatedAt: Date.now() });
}

async function getCachedWardrobe(): Promise<ClothingItem[] | null> {
  const db = await openDB('ydk_offline', 1);
  const row = await db.get('wardrobe', 'list');
  if (!row) return null;
  const days = (Date.now() - row.updatedAt) / (24 * 60 * 60 * 1000);
  return days <= CACHE_DAYS_NORMAL ? row.data : null; // 普通用户 7 天有效
}
```

### 6.4 每日推送与降级（与 PRD 2.3 一致）

**推送方式（网页版）**：使用 **Web Push**（需用户授权）。支持设置每日推送时间（默认 7:00）、开启/关闭推送、选择推送场景（工作日/周末/全部）。

**推送权限关闭时的降级方案**（PRD 2.3.1）：
- 检测用户是否授予推送权限；**未授予时**，首页顶部（天气信息卡片下方）显示**「每日推荐」入口卡片**，点击后自动生成当日推荐（默认场合或最近选择场合 + 当日天气）。
- **已授予推送时**，首页可不展示该入口；用户可在设置中关闭推送，此时首页再显示「每日推荐」入口。
- 首次请求推送权限被拒绝时，友好提示「您可以在首页查看每日推荐」，并在设置中提供「开启推送通知」入口。

---

## 七、维护机制

### 7.1 接口优化与反馈机制

#### 7.1.1 用户修正数据收集
**数据收集**（Go语言实现）：
```go
package feedback

import (
    "context"
    "time"
    "gorm.io/gorm"
)

type FeedbackCollector struct {
    db *gorm.DB
}

type CorrectionFeedback struct {
    ID            uint      `gorm:"primaryKey"`
    ItemID        string    `gorm:"index"`
    UserID        string    `gorm:"index"` // 已匿名化
    APIResult     string    `gorm:"type:json"` // 豆包AI返回的原始结果
    Corrected     string    `gorm:"type:json"` // 用户修正后的结果
    Timestamp     time.Time
}

func (f *FeedbackCollector) CollectCorrection(ctx context.Context, itemID string, apiResult, corrected map[string]interface{}) error {
    // 匿名化用户ID
    anonymizedUserID := anonymizeUserID(userID)
    
    // 转换为JSON
    apiResultJSON, _ := json.Marshal(apiResult)
    correctedJSON, _ := json.Marshal(corrected)
    
    feedback := &CorrectionFeedback{
        ItemID:    itemID,
        UserID:    anonymizedUserID,
        APIResult: string(apiResultJSON),
        Corrected: string(correctedJSON),
        Timestamp: time.Now(),
    }
    
    // 保存到数据库
    if err := f.db.WithContext(ctx).Create(feedback).Error; err != nil {
        return err
    }
    
    // 可选：反馈给豆包AI团队（如支持）
    if f.shouldFeedbackToDoubao() {
        go f.sendFeedbackToDoubao(ctx, feedback)
    }
    
    return nil
}
```

#### 7.1.2 接口参数优化
**参数调优**（Go语言实现）：
```go
package optimizer

type APIOptimizer struct {
    db *gorm.DB
}

type OptimizedOptions struct {
    EnhancedMode     bool `json:"enhanced_mode"`
    AdditionalChecks bool `json:"additional_checks"`
}

func (o *APIOptimizer) OptimizeRecognitionParams(ctx context.Context, userFeedbackData []CorrectionFeedback) (map[string]OptimizedOptions, error) {
    // 分析用户修正模式
    correctionPatterns, err := o.analyzeCorrections(ctx, userFeedbackData)
    if err != nil {
        return nil, err
    }
    
    // 针对低准确率品类，调整识别选项
    optimizedOptions := make(map[string]OptimizedOptions)
    for category, accuracy := range correctionPatterns {
        if accuracy < 0.9 { // 准确率低于90%
            optimizedOptions[category] = OptimizedOptions{
                EnhancedMode:     true,  // 启用增强模式
                AdditionalChecks: true,  // 增加额外检查
            }
        }
    }
    
    return optimizedOptions, nil
}
```

#### 7.1.3 结果后处理优化
**后处理策略**（Go语言实现）：
```go
package processor

type ResultPostProcessor struct {
    db *gorm.DB
}

func (p *ResultPostProcessor) PostProcess(ctx context.Context, apiResult map[string]interface{}, userPreferences map[string]interface{}, correctionHistory []CorrectionFeedback) map[string]interface{} {
    // 1. 置信度过滤
    category, ok := apiResult["category"].(map[string]interface{})
    if ok {
        confidence, _ := category["confidence"].(float64)
        if confidence < 0.8 {
            apiResult["needs_manual_review"] = true
        }
    }
    
    // 2. 基于用户历史修正的调整
    if len(correctionHistory) > 0 {
        apiCategory, _ := category["name"].(string)
        for _, correction := range correctionHistory {
            var correctionData map[string]interface{}
            json.Unmarshal([]byte(correction.APIResult), &correctionData)
            if correctionCategory, ok := correctionData["category"].(map[string]interface{}); ok {
                if correctionCategory["name"] == apiCategory {
                    // 降低置信度
                    confidence := category["confidence"].(float64)
                    category["confidence"] = confidence * 0.9
                }
            }
        }
    }
    
    // 3. 基于用户偏好的调整
    if preferredCategories, ok := userPreferences["preferred_categories"].([]interface{}); ok {
        apiCategory, _ := category["name"].(string)
        for _, preferred := range preferredCategories {
            if preferred == apiCategory {
                // 提升置信度
                confidence := category["confidence"].(float64)
                category["confidence"] = confidence * 1.1
                break
            }
        }
    }
    
    return apiResult
}
```

### 7.2 规则库更新流程

#### 7.2.1 人工维护流程
**更新周期**：每季度更新一次

**更新流程**：
1. **内容团队收集**：时尚趋势、用户反馈、搭配案例
2. **专业顾问审核**：穿搭顾问团队审核和制定更新方案
3. **技术团队实施**：更新规则库、模板、模型
4. **灰度测试**：小范围用户测试验证
5. **全量发布**：全量更新

**规则更新接口**：
```python
class RuleLibraryManager:
    def update_rules(self, new_rules):
        """更新规则库"""
        # 1. 验证规则格式
        validated_rules = self.validate_rules(new_rules)
        
        # 2. 备份当前规则
        self.backup_current_rules()
        
        # 3. 更新数据库
        for rule in validated_rules:
            self.db.upsert_rule(rule)
        
        # 4. 清除缓存
        self.clear_cache()
        
        # 5. 记录更新日志
        self.log_update(new_rules, updated_by='admin')
```

#### 7.2.2 规则效果评估
**A/B测试框架**：
```python
class ABTestFramework:
    def create_experiment(self, name, variants):
        """创建A/B测试实验"""
        experiment = {
            'name': name,
            'variants': variants,  # [{'name': 'A', 'rules': {...}}, {'name': 'B', 'rules': {...}}]
            'traffic_split': 0.5,  # 50%用户使用A，50%使用B
            'metrics': ['收藏率', '应用率', '满意度']
        }
        return experiment
    
    def assign_variant(self, user_id, experiment):
        """为用户分配变体"""
        # 基于用户ID哈希，确保一致性
        hash_value = hash(f"{user_id}_{experiment['name']}")
        variant_index = hash_value % len(experiment['variants'])
        return experiment['variants'][variant_index]
    
    def analyze_results(self, experiment):
        """分析A/B测试结果"""
        results = {}
        for variant in experiment['variants']:
            metrics = self.collect_metrics(variant['name'])
            results[variant['name']] = metrics
        
        # 统计显著性检验
        winner = self.statistical_test(results)
        return winner
```

### 7.3 用户行为反馈闭环

#### 7.3.1 数据收集系统
**埋点设计**：
```python
class AnalyticsTracker:
    def track_event(self, event_name, properties):
        """追踪用户行为事件"""
        event = {
            'event_name': event_name,
            'properties': properties,
            'user_id': anonymize_user_id(user_id),
            'timestamp': current_timestamp(),
            'device_info': get_device_info()
        }
        # 实时上报（关键事件）或批量上报（非关键事件）
        if event_name in CRITICAL_EVENTS:
            self.report_realtime(event)
        else:
            self.add_to_batch_queue(event)
```

**核心事件**：
```python
CRITICAL_EVENTS = [
    'upload_success',
    'recommendation_viewed',
    'recommendation_favorited',
    'recommendation_applied',
    'item_replaced'
]

# 事件示例
tracker.track_event('recommendation_favorited', {
    'outfit_id': 'outfit_123',
    'occasion': '职场通勤',
    'weather': '晴天',
    'recommendation_score': 0.85
})
```

#### 7.3.2 反馈分析
**数据分析看板**：
```python
class AnalyticsDashboard:
    def get_daily_metrics(self, date):
        """获取每日指标"""
        return {
            'upload_rate': self.calculate_upload_rate(date),
            'recommendation_ctr': self.calculate_ctr(date),
            'favorite_rate': self.calculate_favorite_rate(date),
            'replace_rate': self.calculate_replace_rate(date)
        }
    
    def identify_issues(self):
        """识别问题"""
        issues = []
        
        # 检查上传率下降
        if self.upload_rate_trend < -0.2:  # 下降20%
            issues.append({
                'type': 'upload_rate_decline',
                'severity': 'high',
                'suggestion': '检查上传流程或网络问题'
            })
        
        # 检查推荐点击率下降
        if self.recommendation_ctr_trend < -0.15:
            issues.append({
                'type': 'recommendation_quality_decline',
                'severity': 'medium',
                'suggestion': '优化推荐算法或内容'
            })
        
        return issues
```

#### 7.3.3 迭代优化流程
**优化闭环**：
```
1. 数据收集 → 2. 问题识别 → 3. 方案制定 → 4. A/B测试 → 5. 效果评估 → 6. 全量发布
     ↑                                                                          ↓
     └─────────────────────────── 持续监控 ←──────────────────────────────────┘
```

**实现**：
```python
class OptimizationLoop:
    def run_weekly_analysis(self):
        """每周数据分析"""
        # 1. 收集数据
        metrics = self.collect_weekly_metrics()
        
        # 2. 识别问题
        issues = self.identify_issues(metrics)
        
        # 3. 制定优化方案
        for issue in issues:
            solution = self.propose_solution(issue)
            # 4. 创建A/B测试
            experiment = self.create_ab_test(solution)
            self.run_experiment(experiment)
    
    def evaluate_experiment(self, experiment_id):
        """评估实验效果"""
        results = self.get_experiment_results(experiment_id)
        if results['improvement'] > 0.05:  # 提升5%以上
            # 5. 全量发布
            self.deploy_solution(experiment_id)
        else:
            # 回滚或调整
            self.rollback_experiment(experiment_id)
```

---

## 八、技术架构总结

### 8.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    衣搭库网页版（浏览器）                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ 前端应用     │  │ IndexedDB    │  │ Web Push（可选）  │  │
│  │(React/Vue等) │  │ 离线/队列缓存 │  │ 每日推荐推送      │  │
│  └──────┬───────┘  └──────┬───────┘  └────────┬───────────┘  │
└─────────┼──────────────────┼──────────────────┼──────────────┘
          │                  │                  │
          │  前端预处理：图片压缩/匿名化/分块    │
          └──────────────────┴──────────────────┘
                           │ HTTPS
        ┌──────────────────┴──────────────────┐
        │        接口适配层                     │
        │  ┌──────────────────────────────┐   │
        │  │ 统一API封装/异常重试/数据加密  │   │
        │  │ 结果解析/格式转换/缓存管理     │   │
        │  └──────────────┬───────────────┘   │
        └──────────────────┼───────────────────┘
                           │ HTTPS
        ┌──────────────────┴──────────────────┐
        │      豆包AI开放接口                  │
        │  ┌──────────────┐  ┌─────────────┐ │
        │  │ 衣物识别能力  │  │ 穿搭推荐能力 │ │
        │  └──────────────┘  └─────────────┘ │
        └──────────────────┬──────────────────┘
                           │
        ┌──────────────────┴──────────────────┐
        │        接口适配层（结果处理）          │
        │  ┌──────────────────────────────┐   │
        │  │ 结果解析/格式转换/缓存        │   │
        │  └──────────────┬───────────────┘   │
        └──────────────────┼───────────────────┘
                           │
        ┌──────────────────┴──────────────────┐
        ┌──────────────────┴──────────────────┐
        │              数据存储层               │
        │  ┌──────────┐  ┌──────────┐        │
        │  │  MySQL   │  │  Redis   │        │
        │  │(业务数据)│  │  (缓存)   │        │
        │  └──────────┘  └──────────┘        │
        └──────────────────┬──────────────────┘
                           │
        ┌──────────────────┴──────────────────┐
        │           七牛云OSS存储               │
        │  ┌──────────────────────────────┐   │
        │  │  图片文件存储（加密可选）      │   │
        │  └──────────────────────────────┘   │
        └─────────────────────────────────────┘
```

**核心架构流程**：
- **MVP**：上传后手动分类，豆包AI 仅使用**穿搭推荐能力**；图片流程为：前端预处理 → 上传至后端/OSS → 用户编辑品类等 → 保存。
- **后续（接入 AI 识别后）**：如下全流程。
```
衣搭库网页版（浏览器）
    ↓（前端预处理：图片压缩/匿名化/分块）
接口适配层（统一API封装/异常重试/数据加密）
    ↓（HTTPS加密传输）
豆包AI开放接口（衣物识别+穿搭推荐能力）
    ↓（返回结构化结果）
接口适配层（结果解析/格式转换/缓存）
    ↓
衣搭库网页版（结果展示/用户交互/IndexedDB 缓存与云端同步）
```

### 8.2 关键技术选型总结

| 模块 | 技术选型 | 理由 |
|------|---------|------|
| **前端** | 现代浏览器 + React/Vue/Next.js 等 + TypeScript | 网页版优先交付，响应式与 PWA 可选 |
| **AI服务** | 豆包AI开放接口 | 无需自建模型，降低开发运维成本，准确率高 |
| **接口适配层** | Go 语言（与后端统一） | 统一 API 封装，异常重试，数据加密，结果解析 |
| **后端框架** | Go语言（Gin/Echo） | 高性能，并发能力强，适合API服务 |
| **数据库** | MySQL | 成熟稳定的关系型数据库，支持JSON字段 |
| **缓存** | Redis | 高性能内存缓存，减少API调用 |
| **文件存储** | 七牛云OSS | 国内 CDN 加速，上传下载速度快 |
| **图片处理** | 前端预处理 | 浏览器内压缩、匿名化、分块（1MB/块） |

### 8.3 性能指标总结

| 指标 | 目标值 | 监控方式 |
|------|--------|---------|
| **上传成功率** | ≥99%（断点续传后） | 实时监控 |
| **上传耗时** | ≤3秒（4G 网络，单张） | P95 耗时统计 |
| **推荐生成** | ≤3秒（P95） | 接口响应时间 |
| **页面加载** | 首屏可交互时间合理 | 前端性能监控 |
| **识别准确率**（后续版本） | 上衣/裤子/裙子/鞋子≥95%<br>配饰≥80%<br>颜色≥90%<br>风格≥85%<br>季节≥90%<br>用户修正率≤10% | 测试集评估+用户修正率 |
| **识别单张耗时**（后续版本） | ≤5秒（主流浏览器） | 接口耗时统计 |

---

## 九、后续迭代方向

### 9.1 Phase 2 / Phase 3 功能扩展

#### 9.1.0 AI 衣物识别（后续版本，MVP 为手动分类）
**范围**：接入豆包AI 衣物识别能力，用户上传照片后由 AI 自动识别品类、颜色、风格、季节等，用户可确认或修正后保存。  
**技术**：本章第二节「豆包AI接口集成」中 2.3.1 衣物识别接口、2.4 接口适配层（RecognizeClothing）、2.5 识别流程、2.6 准确率监控及 3.5 识别中断处理已预留设计与接口规范，后续按该方案接入即可。

#### 9.1.1 日历读取集成（PRD 标注为 Phase 3 可选）
**技术实现**：
- **网页版**：可考虑 Web 日历 API（如 Google Calendar API、Outlook 等）或浏览器日历集成（若标准支持），或延后至 App 版再实现
- 仅读取未来 7 天内的日程标题和描述，识别场合关键词（会议、约会、运动等）
- PRD：MVP 版本聚焦手动选择场合，通过快捷入口降低对日历的依赖

#### 9.1.2 虚拟试穿功能（PRD Phase 3 埋点）
**技术方案**：
- 允许用户临时添加外部单品到「虚拟衣橱」（标记为未购买）
- 基于虚拟衣橱生成搭配方案，标注「包含未购买单品」
- 可为后续购物功能提供数据积累；网页版可采用图像合成或 2D 拼图展示，AR 能力可留待 App 版（ARKit/ARCore）

### 9.2 算法优化方向

1. **深度学习推荐**：使用GNN（图神经网络）建模衣物关系
2. **强化学习**：基于用户反馈优化推荐策略
3. **多模态融合**：结合图像、文本、用户行为多维度推荐

---

**文档版本**：v1.2（MVP 手动分类，AI 识别延后）  
**创建日期**：2024  
**最后更新**：2025  
**变更说明**：**MVP 版本**改为用户上传后**手动分类**（品类/颜色/风格/季节），**AI 衣物识别**规划在**后续版本**接入，相关接口与适配层设计保留；其余根据《衣搭库产品需求文档》为网页版优先，涵盖前端预处理、断点续传（1MB/块）、上传队列与状态、已淘汰与推送降级、离线缓存与 Web Push、数据存储与多设备等与 PRD 一致的技术实现。
