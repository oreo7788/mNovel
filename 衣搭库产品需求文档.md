# 衣搭库 App 产品需求文档（PRD）

## 一、产品概述

### 1.1 产品定位
衣搭库是一款基于 AI 视觉识别的智能穿搭助手 App，帮助用户建立专属电子衣橱，通过 AI 算法结合天气、场合、用户偏好，生成个性化每日穿搭推荐，解决"衣服满柜却不知道怎么搭""每天出门纠结穿什么"的痛点。

### 1.2 目标用户
- **主要用户**：20-35 岁的职场人、学生
- **用户特征**：追求简约高效的穿搭方式，喜欢个性化风格，拒绝千篇一律的搭配模板
- **使用场景**：每日出门前穿搭决策、衣橱整理、风格探索

### 1.3 核心价值
- **基于真实衣橱，给你最可行的每日穿搭**：与其他穿搭推荐 App 不同，衣搭库不推荐你买不到的"理想搭配"，而是基于你衣橱里真实存在的衣物，生成你当下就能穿的搭配方案。每一套推荐都来自你的衣橱，每一件单品你都有，真正做到"推荐即可用"
- **告别"看得到穿不了"的尴尬**：不再需要对着网上的穿搭模板翻遍衣柜找相似款，AI 直接告诉你"用你现有的这件上衣 + 那条裤子 + 这双鞋"就能搭出理想效果
- **3 步完成，即刻可用**：上传-识别-推荐，快速建立专属电子衣橱，AI 结合天气、场合、你的偏好，从你的真实衣橱中智能匹配，生成最符合你需求的穿搭方案

---

## 二、核心功能详情

### 2.1 电子衣橱管理功能

#### 2.1.1 衣物上传
**功能描述**：
- 支持单张/批量上传衣服照片（最多 9 张/次）
- 支持从相册选择或直接拍照
- 上传时可选择衣物所属品类（可选，AI 会自动识别）

**AI 识别能力**：
- **品类识别**：上衣、裤子、裙子、鞋子、配饰（帽子/包包/腰带等）
- **颜色识别**：主色调 + 辅助色（最多 3 种颜色）
- **风格识别**：休闲、通勤、运动、正式、甜美、中性、复古等
- **季节属性**：春夏、秋冬、四季通用

**识别准确率要求**：
- **品类识别准确率（按品类拆分）**：
  - 上衣、裤子、裙子、鞋子：≥95%
  - 配饰（帽子/包包/腰带等）：≥80%（配饰品类多样，识别难度较高）
- **颜色识别准确率**：≥90%
- **风格识别准确率**：≥85%
- **季节属性识别准确率**：≥90%

**测试集来源**：
- **内部测试集**：团队收集的标准化衣物照片（各品类各 500+ 张，涵盖不同角度、光线、背景）
- **用户真实上传样本**：从 Beta 测试用户中匿名收集真实上传样本（需用户授权），建立持续更新的测试集，用于验证豆包 AI 在真实场景下的表现和优化 Prompt

**用户修正率（辅助指标）**：
- **目标**：≤10% 的衣物需要用户手动修正识别结果
- **统计方式**：统计用户上传后 7 天内进行过修正的衣物比例
- **意义**：用户修正率越低，说明识别准确率越高，用户体验越好。当修正率 >10% 时，需要优化豆包 AI 的 Prompt 提示词和识别策略

**交互设计**：
- 上传后显示上传进度（0-50%）和识别进度（50-100%）
- 识别过程中显示"AI 正在识别中..."提示
- 识别完成后展示识别结果卡片，用户可查看/编辑
- 识别失败时显示友好提示，支持重新识别

**网络中断处理与断点续传**：
- **断点续传支持**：
  - 支持照片上传断点续传功能，上传中断后可从断点处继续上传，无需重新上传已传输部分
  - 使用分块上传机制，将照片分割为多个数据块（建议 1MB/块），记录每个数据块的上传状态
  - 上传进度实时保存到本地数据库，记录已上传的数据块位置
- **上传中断处理**：
  - **网络中断时**：已选择的照片保存在本地队列，显示"等待上传"状态
  - **已上传数据块**：已成功上传的数据块记录在本地，网络恢复后从断点处继续上传
  - **未上传数据块**：未上传的数据块等待网络恢复后继续上传
- **自动恢复机制**：
  - **网络恢复检测**：系统自动检测网络连接状态，网络恢复后立即触发断点续传
  - **自动恢复提示**：网络恢复后，如检测到有待恢复的上传任务，自动弹出提示弹窗
  - **弹窗内容**：
    - 标题："正在恢复上传"
    - 内容：显示"检测到 X 张照片未完成上传，正在自动恢复..."（X 为待恢复照片数量）
    - 进度显示：显示当前恢复进度（如"正在恢复第 2/5 张照片"）
    - 取消按钮：用户可点击"取消"关闭弹窗（上传在后台继续）
  - **后台继续上传**：弹窗关闭后，上传任务在后台继续执行，用户可正常使用其他功能
- **上传队列管理**：
  - **上传队列页面**：用户可在"上传队列"页面查看所有待上传照片（包括中断的照片）
  - **状态显示**：
    - "等待上传"：未开始上传的照片
    - "上传中"：正在上传的照片（显示进度百分比）
    - "恢复中"：网络恢复后正在断点续传的照片（显示"正在恢复上传"状态）
    - "上传失败"：上传失败的照片（显示失败原因，如"网络错误"）
  - **手动重试**：用户可在上传队列页面手动触发重试（支持单个或批量重试）
  - **取消上传**：用户可取消待上传或上传中的照片（已上传的数据块保留，下次可继续）
- **识别中断处理**：
  - **网络中断时**：已上传但未完成识别的照片保存在识别队列，显示"等待识别"状态
  - **识别任务管理**：
    - 识别任务与上传任务独立管理，上传完成后自动触发识别任务
    - 识别请求失败时自动加入重试队列，网络恢复后自动重试
    - 支持手动重新识别（用户可点击"重新识别"按钮）
  - **识别结果保存**：识别完成后立即保存到本地，避免数据丢失
- **提示机制**：
  - **网络中断时**：显示友好提示"网络连接已断开，照片已保存，网络恢复后将自动上传"
  - **网络恢复时**：自动弹出"正在恢复上传"弹窗，显示恢复进度
  - **上传完成时**：弹窗自动关闭，显示成功提示"X 张照片上传完成"
  - **上传失败时**：显示失败提示"部分照片上传失败，请检查网络后重试"，提供重试入口

#### 2.1.2 识别结果修正
**功能描述**：
- 支持手动修正品类、颜色、风格、季节属性
- 支持添加自定义标签（如"显瘦""百搭""新衣服""面试穿""约会专用"等）
- 支持添加备注文字（最多 200 字）

**交互设计**：
- 识别结果卡片支持点击进入编辑页面
- 编辑页面采用表单式布局，分类清晰
- 保存后返回衣橱列表

#### 2.1.3 衣橱筛选与管理
**功能描述**：
- **多维度筛选**：
  - 按品类筛选（上衣/裤子/裙子/鞋子/配饰）
  - 按风格筛选（休闲/通勤/运动/正式等）
  - 按季节筛选（春夏/秋冬/四季）
  - 按标签筛选（自定义标签）
  - 按颜色筛选（颜色色板选择）
- **状态管理**：
  - 支持标记"已淘汰"衣物（不参与推荐，但保留记录）
  - 支持删除衣物（需二次确认）
  - 支持批量操作（批量删除/批量标记）

**快速筛选"已淘汰"衣物入口**：
- **筛选栏快捷按钮**：
  - 在衣橱页面顶部筛选栏中，提供"已淘汰"快捷筛选按钮
  - 按钮位置：筛选栏最左侧或最右侧（突出显示）
  - 按钮样式：与其他筛选按钮一致，但显示"已淘汰"文字和图标
  - 按钮状态：显示已淘汰衣物数量（如"已淘汰(5)"），便于用户了解淘汰衣物数量
- **快速筛选功能**：
  - 点击"已淘汰"按钮后，立即筛选并显示所有已淘汰的衣物
  - 筛选结果实时更新，仅显示已淘汰状态的衣物
  - 支持在已淘汰状态下继续使用其他筛选条件（如"已淘汰 + 上衣"）
- **已淘汰衣物管理**：
  - 在已淘汰筛选状态下，支持批量操作：
    - **批量恢复**：选择多个已淘汰衣物，一键恢复为可用状态
    - **批量删除**：选择多个已淘汰衣物，批量删除（需二次确认）
    - **单个操作**：点击单个衣物卡片，支持恢复或删除
  - 操作提示：恢复或删除后，显示操作成功提示，并自动更新筛选结果
- **快捷入口优势**：
  - 快速定位：用户可快速查看所有已淘汰的衣物，无需逐个查找
  - 批量管理：支持批量恢复或删除，提升管理效率
  - 状态清晰：显示已淘汰数量，帮助用户了解衣橱状态

**边界场景处理**：
- **全部衣物标记"已淘汰"**：
  - 检测到所有衣物均为"已淘汰"状态时，提示用户"所有衣物已标记为已淘汰，无法生成推荐"
  - 提供快捷操作："取消全部已淘汰标记"或"恢复部分衣物"
  - 在推荐页面显示友好提示："您的衣橱中没有可用衣物，请先取消部分衣物的'已淘汰'标记"
  - 引导用户至少保留 10 件可用衣物，才能正常使用推荐功能
- **无已淘汰衣物**：
  - 当用户没有已淘汰衣物时，"已淘汰"按钮显示为灰色（禁用状态）
  - 点击时提示"暂无已淘汰的衣物"

**交互设计**：
- 衣橱页面顶部提供筛选栏（横向滑动）
- **"已淘汰"快捷按钮**：筛选栏中突出显示，显示已淘汰数量
- 筛选结果实时更新
- 支持组合筛选（多条件同时生效，包括"已淘汰"状态）
- **已淘汰筛选状态**：筛选栏中"已淘汰"按钮高亮显示，表示当前处于已淘汰筛选状态

#### 2.1.4 衣橱展示
**功能描述**：
- 默认以网格形式展示（2 列或 3 列，用户可选）
- 每个衣物卡片显示：缩略图、品类图标、颜色标签
- 支持列表视图（显示更多信息：风格、季节、标签）
- 支持搜索功能（按备注文字、标签搜索）

---

### 2.2 AI 自动穿搭推荐功能

#### 2.2.1 用户偏好设置（首次使用引导）
**功能描述**：
- **风格偏好**：多选（休闲、通勤、运动、正式、甜美、中性、复古等）
- **常去场合**：多选（职场通勤、约会、运动、聚会、面试、旅行等）
- **身高体型参考**：输入身高（cm）、体重（kg），用于推荐合身搭配
- **颜色偏好**：选择喜欢的颜色（最多 5 种），用于优先推荐
- **穿搭原则**：多选（显瘦、显高、舒适、正式、时尚等）

**交互设计**：
- 首次打开 App 时弹出引导流程（3-4 步）
- 每步有清晰的说明文字和示例
- 支持跳过（后续可在设置中补充）

#### 2.2.2 手动穿搭推荐
**功能描述**：
- 用户选择"场合 + 天气"组合：
  - **场合**：职场通勤、约会、运动、聚会、面试、旅行、周末逛街等
  - **天气**：晴天、多云、雨天、雪天、高温（>30°C）、低温（<10°C）
- AI 从衣橱中匹配 3-5 套穿搭组合
- 每套穿搭包含：
  - 搭配单品清单（上衣、下装、鞋子、配饰）
  - 搭配亮点说明（如"高腰裤 + 短上衣显比例""同色系搭配显高级"）
  - 适用场景说明
  - 单品图片展示（可点击查看详情）

**场合快速选择（快捷入口）**：
- **首页快捷按钮**：在首页预设常用场合快捷按钮（如"通勤""约会""运动"），用户一键选择即可生成推荐
- **智能默认天气**：快捷按钮自动获取当日天气，无需用户手动选择
- **自定义快捷按钮**：用户可根据个人习惯，在设置中选择首页显示的快捷场合（最多 3 个）
- **降低日历依赖**：通过快捷入口，用户无需依赖日历日程即可快速生成推荐，提升使用效率

**推荐算法逻辑**：
- 优先匹配用户偏好风格
- 考虑天气因素（温度、是否下雨）
- 考虑场合适配度（正式度、舒适度）
- 避免重复推荐（7 天内不重复）

**边界场景处理**：
- **全部衣物标记"已淘汰"**：
  - 检测到所有可用衣物均为"已淘汰"状态时，无法生成推荐
  - 显示提示："您的衣橱中没有可用衣物，请先取消部分衣物的'已淘汰'标记"
  - 提供快捷入口跳转到衣橱管理页面

**冷启动处理（衣橱单品不足时）**：
- **触发条件**：衣橱单品数量 < 10 件，或缺少关键品类（下装、鞋子、上衣）
- **推荐算法策略**：
  - **仅启用规则引擎 + 通用风格推荐**：
    - 规则引擎权重：100%（完全依赖规则引擎，确保推荐符合基本穿搭原则）
    - 通用风格推荐：基于通用穿搭模板和经典搭配规则
    - 不启用协同过滤：用户行为数据不足，无法进行有效的协同过滤推荐
    - 不启用内容推荐：用户衣橱单品过少，内容相似度匹配效果不佳
  - **推荐生成方式**：
    - 优先使用基础搭配模板（经典搭配组合，如"白T+牛仔裤+小白鞋""衬衫+西裤+皮鞋"等）
    - 结合用户已设置的偏好（风格、场合、颜色偏好）进行规则匹配
    - 如用户未设置偏好，使用通用推荐规则
- **基础搭配模板**：
  - 提供经典搭配模板供参考（如"白T+牛仔裤+小白鞋""衬衫+西裤+皮鞋"等）
  - 模板标注"参考模板，非您衣橱单品"，避免用户误解
  - 展示 5-8 套不同场合的基础搭配模板
- **智能品类补齐引导**：
  - 检测缺失的关键品类，智能提示用户补齐
  - 提示文案示例："您缺少下装，建议上传裤子或裙子""您缺少鞋子，建议上传至少 1 双"
  - 提供一键跳转上传功能
- **虚拟试穿功能（Phase 3 埋点）**：
  - 允许用户临时添加外部单品到"虚拟衣橱"（标记为"未购买"状态）
  - 基于虚拟衣橱生成搭配方案，标注"包含未购买单品"
  - 用户可查看完整搭配效果，决定是否购买
  - 为后续购物功能（Phase 3）提供数据积累和用户习惯培养

**交互设计**：
- **正常推荐**：推荐页面采用卡片式布局，每套穿搭一张卡片
- **冷启动状态**：
  - 显示友好的引导页面，避免用户因单品不足而流失
  - 基础搭配模板以卡片形式展示，每套模板包含单品图片和搭配说明
  - 缺失品类提示采用醒目的卡片样式，支持一键跳转上传
  - "虚拟试穿"功能入口明显，标注"Beta 功能"或"即将推出"
- 卡片支持左右滑动查看更多
- 支持收藏/不感兴趣操作

#### 2.2.3 "换一件"功能
**功能描述**：
- 用户对某套穿搭中的单品不满意，可点击该单品
- 弹出同品类其他衣物列表（显示缩略图、颜色、风格）
- 用户选择替换后，AI 重新生成搭配方案
- **AI 重新评估机制**：替换单品后，AI 会重新校验整体方案的风格一致性、颜色搭配协调性，确保新搭配方案的整体协调度
- 支持多次替换（如先换上衣，再换鞋子），每次替换都会重新评估整体方案

**交互设计**：
- 穿搭卡片中的单品支持点击
- 点击后底部弹出替换选择器（半屏弹窗）
- 替换后显示"AI 正在重新评估搭配..."提示
- **替换单品过渡动画**：
  - 替换后使用过渡动画展示新搭配（动画时长 200ms，使用 ease-in-out 缓动曲线）
  - 动画效果：旧单品淡出，新单品淡入，整体搭配卡片平滑过渡
  - 动画完成后标注"已重新优化"，提示用户搭配已更新

---

### 2.3 每日穿搭推送功能

#### 2.3.1 推送设置
**功能描述**：
- 支持设置每日推送时间（默认 7:00，可自定义）
- 支持开启/关闭推送
- 支持选择推送场景（工作日/周末/全部）

**推送权限关闭时的降级方案**：
- **检测推送权限状态**：系统自动检测用户是否授予推送权限
- **首页"每日推荐"入口**：
  - 当用户关闭推送权限时，首页顶部（天气信息卡片下方）显示"每日推荐"入口卡片
  - 卡片样式：与天气信息卡片风格一致，显示"今日推荐"文字和推荐图标
  - 卡片内容：显示"点击查看今日穿搭推荐"提示文字
  - 点击行为：点击后自动生成当日推荐方案（使用默认场合或用户最近选择的场合，自动获取当日天气）
- **推送权限开启时**：
  - 首页不显示"每日推荐"入口（用户可通过推送通知获取推荐）
  - 用户可在设置中手动关闭推送，此时首页会显示"每日推荐"入口
- **权限引导**：
  - 首次使用推送功能时，如用户拒绝推送权限，友好提示"您可以在首页查看每日推荐"
  - 在设置页面提供"开启推送通知"入口，用户可随时开启

#### 2.3.2 自动推荐生成
**功能描述**：
- **天气数据获取**：自动获取用户定位城市的当日天气（温度、天气状况、风力）
- **推荐生成（MVP 版本）**：
  - 根据天气选择合适季节的衣物
  - 结合用户偏好生成 3-5 套推荐（默认场合或用户最近选择的场合）
  - 推送卡片包含：天气信息、推荐理由、搭配方案
- **日程读取（Phase 3 可选功能）**：
  - 支持读取手机日历日程（需用户授权），识别日程中的场合关键词（如"会议""约会""运动"）
  - 根据日程自动选择合适场合的搭配
  - 注意：此功能为 Phase 3 可选功能，MVP 版本聚焦手动选择场合，通过快捷入口降低对日历的依赖

**交互设计**：
- 推送通知点击后跳转到推荐详情页
- 详情页展示完整的穿搭方案和单品清单

#### 2.3.3 收藏与复用
**功能描述**：
- 用户可收藏喜欢的穿搭方案
- 收藏的穿搭保存在"我的收藏"页面
- 支持从收藏中快速应用（一键添加到今日穿搭）
- 支持编辑收藏的穿搭（替换单品、添加备注）

**交互设计**：
- 穿搭卡片右上角显示收藏按钮（心形图标）
- 收藏后按钮变为实心，支持取消收藏
- "我的收藏"页面采用网格布局，支持筛选和搜索

---

## 三、次要功能详情

### 3.1 穿搭记录功能

#### 3.1.1 穿搭日记
**功能描述**：
- 用户可上传当日实际穿搭照片（最多 3 张）
- 自动识别照片中的衣物（匹配衣橱中的单品）
- 支持手动标记搭配的单品
- 支持添加文字记录（心情、场合、他人评价等，最多 500 字）
- 支持添加标签（如"今日最佳""需要改进"）

#### 3.1.2 穿搭回顾
**功能描述**：
- 以日历形式展示历史穿搭记录
- 支持按月份/年份查看
- 支持搜索历史穿搭（按日期、标签、单品）
- 支持统计功能（如"本月最常穿的单品""最喜欢的搭配风格"）

**交互设计**：
- 穿搭记录页面采用时间轴布局
- 每条记录显示日期、照片、单品清单、备注
- 支持点击查看详情和编辑

---

### 3.2 分享功能

#### 3.2.1 穿搭卡片生成
**功能描述**：
- 将穿搭推荐生成为精美的分享卡片
- 卡片包含：
  - 搭配单品清单（图片 + 名称）
  - 搭配亮点文字
  - 适用场景说明
  - App Logo 和水印（可选）

#### 3.2.2 分享渠道
**功能描述**：
- 支持分享到微信、微博、小红书等社交平台
- 支持保存到相册
- 支持复制链接（如后续支持 Web 端查看）

**交互设计**：
- 穿搭卡片右上角显示分享按钮
- 点击后弹出分享选项（系统原生分享菜单）

---

### 3.3 离线查看功能

#### 3.3.1 数据缓存
**功能描述**：
- 自动缓存用户衣橱数据（图片、识别结果、标签等）
- 自动缓存已收藏的穿搭方案
- **推荐方案缓存策略**：
  - **普通用户**：自动缓存最近 7 天的推荐方案（避免占用过多存储空间）
  - **会员用户**：可自定义缓存时长（最多 30 天），支持手动保存推荐方案到收藏
- **存储优化**：超过缓存时长的推荐方案自动清理，已收藏的方案不受影响

#### 3.3.2 离线模式
**功能描述**：
- 无网络时可正常查看衣橱（已识别的衣物数据）
- 无网络时可查看已收藏的穿搭
- 无网络时可查看缓存的推荐方案
- **离线状态下限制**：
  - 不支持上传新照片进行识别（提示用户连接网络）
  - 不支持生成新推荐（提示用户连接网络）
  - 不支持同步云端数据（本地数据正常查看）

**交互设计**：
- 离线状态下显示网络状态提示（顶部横幅）
- 功能按钮根据网络状态启用/禁用

---

## 四、技术实现建议

### 4.1 AI 视觉识别技术

#### 4.1.1 衣物识别方案
**推荐方案**：
- **第三方 AI 服务**：使用**豆包 AI**（字节跳动多模态 AI 服务）进行衣物识别
- **识别能力**：
  - **品类识别**：通过精心设计的 Prompt，引导豆包 AI 识别衣物品类（上衣、裤子、裙子、鞋子、配饰等）
  - **颜色识别**：通过 Prompt 引导豆包 AI 识别主色调和辅助色（最多 3 种颜色）
  - **风格识别**：通过 Prompt 引导豆包 AI 识别衣物风格（休闲、通勤、运动、正式、甜美、中性、复古等）
  - **季节属性**：通过 Prompt 引导豆包 AI 判断季节属性（春夏、秋冬、四季通用），基于颜色、材质、款式特征
- **Prompt 设计原则**：
  - **结构化输出**：Prompt 要求豆包 AI 返回 JSON 格式的结构化数据，便于解析
  - **明确识别要求**：针对不同识别任务设计专用 Prompt，明确识别标准和输出格式
  - **示例引导**：在 Prompt 中提供识别示例，提升识别准确率
  - **错误处理**：Prompt 中包含错误处理逻辑，处理无法识别的情况

**准确率目标**：
- **品类识别**：上衣/裤子/裙子/鞋子 ≥95%，配饰 ≥80%
- **颜色识别**：≥90%
- **风格识别**：≥85%
- **季节属性**：≥90%
- **用户修正率**：≤10%（辅助指标，反映真实用户体验）

**测试集构建**：
- **内部测试集**：团队收集标准化样本（各品类 500+ 张），涵盖不同角度、光线、背景，用于验证豆包 AI 识别准确率
- **真实用户样本**：Beta 测试阶段匿名收集（需授权），建立持续更新的测试集，用于评估豆包 AI 在真实场景下的表现
- **测试集更新机制**：每月更新，纳入用户修正反馈，用于优化 Prompt 提示词和识别策略

**准确率优化**：
- **Prompt 优化**：持续优化豆包 AI 的 Prompt 提示词，提升识别准确率（针对不同品类、场景设计专用 Prompt）
- **参数调优**：调整豆包 AI API 调用参数（如 temperature、max_tokens 等），优化识别结果质量
- **后处理优化**：对豆包 AI 返回的识别结果进行后处理（如结果校验、格式标准化），提升准确率
- **支持模糊照片二次识别**：对模糊照片进行图像增强后再次调用豆包 AI 识别
- **支持用户反馈机制**：识别错误时标记，用于优化 Prompt 和识别策略
- **支持多角度识别**：如用户上传多张照片，综合调用豆包 AI 识别，综合判断结果
- **建立用户修正数据回流机制**：收集用户修正数据，分析识别错误模式，针对性优化 Prompt（重点关注修正率高的品类）

#### 4.1.2 模型部署
**推荐方案**：
- **第三方 AI 服务**：所有 AI 识别任务（品类识别、颜色识别、风格识别、季节属性识别）均对接**豆包 AI**（字节跳动多模态 AI 服务），利用其强大的视觉识别能力
- **豆包 AI 对接方案**：
  - **API 接口**：使用豆包 AI 提供的 RESTful API 接口进行图像识别
  - **多模态能力**：利用豆包 AI 的视觉理解能力，识别衣物的品类、颜色、风格、季节属性等特征
  - **Prompt 工程**：通过精心设计的 Prompt 提示词，引导豆包 AI 准确识别衣物特征并返回结构化数据
  - **批量识别**：支持批量调用 API（一次请求识别多张照片，提升效率）
  - **识别结果缓存**：相同照片的识别结果可缓存，避免重复调用 API（节省成本）
- **API 调用规范**：
  - **请求格式**：按照豆包 AI API 规范构建请求（包含图片 Base64 编码或图片 URL、Prompt 提示词）
  - **响应解析**：解析豆包 AI 返回的 JSON 格式响应，提取结构化识别结果（品类、颜色、风格、季节）
  - **结果映射**：将豆包 AI 返回的识别结果映射为 App 内部数据结构
- **错误处理与重试机制**：
  - **网络错误重试**：API 调用失败时自动重试（最多重试 3 次，指数退避策略）
  - **超时处理**：API 请求超时（默认 30 秒）时自动重试或提示用户
  - **服务降级**：豆包 AI 服务不可用时，提示用户稍后重试，支持手动重新识别
  - **识别失败处理**：识别失败时返回详细错误信息，支持用户手动输入或重新上传
  - **API 限流处理**：检测到 API 限流时，自动降低请求频率，加入请求队列等待重试
- **性能优化**：
  - **图片压缩**：上传前自动压缩图片（保持质量的同时减小体积，加快上传速度，降低 API 调用成本）
  - **并发控制**：同时最多 3 个识别请求并发执行（避免占用过多网络资源，符合 API 限流要求）
  - **请求队列**：识别请求加入队列管理，网络恢复后自动继续
  - **成本优化**：通过识别结果缓存、图片压缩等方式降低 API 调用成本
- **优势**：
  - **无需自建模型**：直接使用豆包 AI 的成熟模型，降低开发和维护成本
  - **模型持续优化**：豆包 AI 持续优化模型，识别准确率不断提升
  - **快速接入**：使用标准 API 接口，快速接入，缩短开发周期
  - **稳定可靠**：豆包 AI 提供稳定的服务，支持高并发请求
  - **多模态能力**：利用豆包 AI 强大的视觉理解能力，支持复杂的衣物识别任务

### 4.2 穿搭推荐算法

#### 4.2.1 推荐引擎
**推荐方案**：
- **规则引擎**：基于用户偏好、天气、场合的规则匹配（如"通勤场合 + 晴天 = 选择通勤风格 + 轻薄材质"）
- **协同过滤**：基于用户行为数据（收藏、应用、不感兴趣）优化推荐
- **内容推荐**：基于衣物特征（颜色、风格、季节）的相似度匹配
- **深度学习**：使用神经网络学习搭配规律（如使用 GNN 建模衣物关系）

**算法权重占比**：
- **标准权重（用户衣橱 ≥ 10 件，有基础行为数据）**：
  - **规则引擎：60%**（作为基础推荐逻辑，确保推荐符合基本穿搭原则和场合适配）
  - **内容推荐：30%**（基于衣物特征匹配，提供多样化推荐）
  - **协同过滤：10%**（基于相似用户行为，提供个性化参考推荐）
  - **说明**：此权重配置确保推荐方案既符合穿搭规则，又具有多样性和一定的个性化
- **冷启动阶段（用户衣橱 < 10 件）**：
  - **仅启用规则引擎 + 通用风格推荐**：
    - 规则引擎：100%（完全依赖规则引擎，确保推荐符合基本穿搭原则）
    - 通用风格推荐：基于通用穿搭模板和经典搭配规则（如"白T+牛仔裤+小白鞋"等）
    - 不启用协同过滤：用户行为数据不足，无法进行有效的协同过滤推荐
    - 不启用内容推荐：用户衣橱单品过少，内容相似度匹配效果不佳
  - **推荐策略**：
    - 优先使用基础搭配模板（经典搭配组合）
    - 结合用户已设置的偏好（风格、场合、颜色偏好）进行规则匹配
    - 如用户未设置偏好，使用通用推荐规则
- **逐步引入协同过滤（积累用户行为后）**：
  - **阶段一（用户行为数据 < 10 次）**：
    - 规则引擎：70%
    - 内容推荐：30%
    - 协同过滤：0%（行为数据不足，暂不启用）
  - **阶段二（用户行为数据 10-30 次）**：
    - 规则引擎：65%
    - 内容推荐：25%
    - 协同过滤：10%（开始引入，权重较低）
  - **阶段三（用户行为数据 ≥ 30 次）**：
    - 规则引擎：60%（标准权重）
    - 内容推荐：30%（标准权重）
    - 协同过滤：10%（标准权重）
- **动态权重调整（有充足行为数据后）**：
  - 根据用户行为数据（收藏率、应用率、不感兴趣率）动态微调权重（±5%）
  - 用户行为数据充足时（≥50 次交互），可适当提升协同过滤权重至 15%
  - 通过 A/B 测试验证权重调整效果
  - 支持人工干预调整权重（针对特定用户群体）

**算法流程**：
1. **判断用户状态**：
   - 检测用户衣橱单品数量
   - 检测用户行为数据积累情况
   - 根据状态选择对应的权重配置（冷启动/阶段一/阶段二/标准权重）
2. **冷启动阶段流程（衣橱 < 10 件）**：
   - 根据场合和天气筛选候选衣物（过滤不合适的品类、季节、风格）
   - 使用规则引擎匹配通用风格推荐（经典搭配模板）
   - 结合用户偏好（如已设置）进行规则匹配
   - 生成 3-5 套推荐方案（基于规则引擎评分排序）
3. **标准推荐流程（衣橱 ≥ 10 件）**：
   - 根据场合和天气筛选候选衣物（过滤不合适的品类、季节、风格）
   - 根据用户偏好排序候选衣物（优先推荐偏好风格、颜色）
   - 组合生成搭配方案（考虑颜色搭配、风格统一、场合适配）
   - 多算法融合评分：
     - 规则引擎评分 × 规则引擎权重（60%）
     - 内容推荐评分 × 内容推荐权重（30%）
     - 协同过滤评分 × 协同过滤权重（10%，如行为数据充足）
     - 综合得分 = 各算法评分加权求和
   - 按综合得分排序，选择 Top 3-5 套推荐方案
4. **去重和多样性保证**（避免重复推荐，保证方案多样性）

**算法迭代优化**：
- **用户行为反馈**：实时收集用户对推荐方案的行为（收藏、应用、不感兴趣），用于优化推荐权重
- **个性化学习**：基于用户历史穿搭记录，学习用户个人搭配偏好，提升推荐个性化程度
- **冷启动到标准推荐的过渡**：
  - 实时监控用户衣橱单品数量和行为数据积累情况
  - 当用户衣橱 ≥ 10 件时，自动从冷启动模式切换到标准推荐模式
  - 当用户行为数据积累到 10 次、30 次时，逐步提升协同过滤权重
  - 确保推荐质量平滑过渡，用户体验不受影响
- **A/B 测试**：定期进行推荐算法 A/B 测试，验证优化效果，确保推荐质量持续提升

#### 4.2.2 搭配规则库
**推荐方案**：
- 建立颜色搭配规则库、风格搭配规则库、场合适配规则库
- 支持用户自定义规则（如"我不喜欢红色 + 绿色搭配"）

**具体规则示例**：

**1. 颜色搭配规则库**：
- **同色系搭配**：上衣和下装选择同一色系的不同深浅（如浅蓝上衣 + 深蓝裤子）
- **对比色搭配**：选择色环上相对的颜色（如蓝色 + 橙色，需谨慎使用）
- **中性色 + 亮色**：中性色（黑/白/灰/米色）作为主色，亮色作为点缀（如白色T恤 + 红色配饰）
- **三色原则**：一套搭配不超过 3 种主要颜色
- **避免冲突色**：避免红色 + 绿色、紫色 + 黄色等冲突色组合（除非用户明确偏好）

**2. 风格搭配规则库**：
- **风格统一原则**：同一套搭配中，单品风格应保持一致（如休闲风格 + 休闲风格）
- **风格混搭规则**：
  - 休闲 + 运动：允许混搭（如休闲T恤 + 运动裤）
  - 通勤 + 正式：允许混搭（如通勤衬衫 + 正式西裤）
  - 甜美 + 中性：谨慎混搭，需考虑颜色协调
- **风格层次**：内搭、外搭、下装风格应协调统一

**3. 场合适配规则库**：
- **职场通勤**：
  - 风格要求：正式风格或通勤风格
  - 颜色要求：深色系优先（黑/灰/深蓝/棕色），避免过于鲜艳
  - 材质要求：上下装材质统一（如都是棉质或都是化纤）
  - 单品要求：避免过于休闲的单品（如运动鞋、破洞牛仔裤）
- **约会场合**：
  - 风格要求：甜美、优雅或时尚风格
  - 颜色要求：可适当使用亮色或柔和色系（如粉色、米色、浅蓝）
  - 单品要求：可选择裙子、有设计感的上衣
- **运动场合**：
  - 风格要求：运动风格
  - 材质要求：透气、吸汗材质（如运动T恤 + 运动裤）
  - 单品要求：运动鞋、运动裤/运动裙
- **面试场合**：
  - 风格要求：正式风格
  - 颜色要求：深色系（黑/深蓝/深灰），避免过于鲜艳或花哨
  - 单品要求：正装或商务休闲装，避免过于休闲
- **周末逛街**：
  - 风格要求：休闲风格
  - 颜色要求：相对自由，可适当使用亮色
  - 单品要求：舒适为主，可选择休闲鞋、牛仔裤等

**4. 季节适配规则库**：
- **春夏**：轻薄材质、浅色系、短袖/短裤/裙子
- **秋冬**：厚实材质、深色系、长袖/长裤、外套
- **四季通用**：中性色、中等厚度材质

**规则库维护**：
- **人工更新**：每季度由专业穿搭顾问团队审核和更新规则库，根据时尚趋势调整
- **用户行为学习**：基于用户收藏和应用数据，自动发现新的搭配规律，补充规则库
- **规则权重动态调整**：根据用户行为反馈，动态调整不同规则的权重和优先级

### 4.3 数据存储与隐私保护

#### 4.3.1 数据存储方案
**推荐方案**：
- **本地存储**：用户照片、识别结果、标签等存储在设备本地（SQLite / Realm）
- **云端同步**：使用加密云端存储（如 iCloud / Google Drive / 自建加密存储），支持多设备同步
- **数据加密**：敏感数据（照片、用户信息）使用 AES-256 加密存储

**多设备同步冲突处理**：
- **冲突检测机制**：
  - 使用版本号（Version）或时间戳（Timestamp）标记每次编辑
  - 同步时检测同一衣物的不同版本，识别编辑冲突
- **冲突解决策略**：
  - **最后写入获胜（Last Write Wins）**：默认采用最后修改时间较新的版本
  - **用户选择**：检测到冲突时，提示用户选择保留哪个设备的版本
  - **合并策略**：对于可合并的字段（如标签），自动合并两个版本的修改（如手机添加标签A，平板添加标签B，合并为A+B）
- **冲突提示**：
  - 检测到冲突时，在设备上显示通知"检测到编辑冲突，请选择保留的版本"
  - 展示冲突对比：显示两个设备的不同修改内容，用户可选择保留
  - 自动处理：对于非关键字段（如备注），采用最后写入获胜策略，无需用户干预
- **同步队列**：
  - 本地修改先保存到本地，加入同步队列
  - 网络恢复后按时间顺序同步，避免并发冲突
  - 同步失败时保留在队列中，下次同步时重试

#### 4.3.2 隐私保护措施
**推荐方案**：
- **第三方 AI 识别服务**：用户照片通过 API 调用豆包 AI 进行识别，识别完成后照片可选择保留在云端或仅保留识别结果
- **照片存储策略**（用户可选择）：
  - **仅识别模式**：照片上传识别后，仅保留识别结果在本地，照片从云端删除（默认模式，保护隐私）
  - **云端备份模式**：照片识别后保留在云端加密存储，支持多设备同步（用户需主动开启）
- **匿名化处理**：上传至云端识别的照片需匿名化处理（去除 EXIF 信息、人脸模糊、去除位置信息）
- **加密传输**：所有照片上传使用 HTTPS 加密传输，确保传输安全
- **用户授权**：明确告知用户数据使用方式，获取用户授权
- **数据删除**：支持用户一键删除所有数据（符合 GDPR 要求）

### 4.4 天气与日程集成

#### 4.4.1 天气数据获取
**推荐方案**：
- 使用第三方天气 API（如和风天气、OpenWeatherMap）
- 根据用户定位自动获取天气（需用户授权位置权限）
- 支持手动选择城市（如用户出差）

#### 4.4.2 日程读取（Phase 3 可选功能）
**推荐方案**：
- **iOS**：使用 EventKit 框架读取日历日程
- **Android**：使用 CalendarContract 读取日历日程
- 解析日程标题和描述，识别场合关键词（如"会议""约会""运动"）
- 需用户授权日历权限，明确告知使用目的
- **注意**：此功能为 Phase 3 可选功能，MVP 版本通过"场合快速选择"快捷入口降低对日历的依赖，用户可手动选择场合，无需读取日历

### 4.5 性能优化

#### 4.5.1 图片处理优化
**推荐方案**：
- 上传时自动压缩图片（保持质量的同时减小体积）
- 使用缩略图技术（列表显示缩略图，详情页显示原图）
- 支持图片懒加载（列表滚动时按需加载）

#### 4.5.2 上传断点续传技术实现
**推荐方案**：
- **分块上传机制**：
  - 将照片文件分割为多个数据块（建议 1MB/块，可根据网络状况动态调整）
  - 每个数据块独立上传，支持单独重试
  - 使用 MD5 校验确保数据块完整性
- **断点续传实现**：
  - **上传进度记录**：实时记录每个数据块的上传状态（未上传/上传中/已上传）
  - **本地状态存储**：上传进度保存到本地数据库（SQLite），记录文件ID、总块数、已上传块数、每个块的状态
  - **断点恢复**：网络恢复后，从本地数据库读取上传进度，从未上传或上传失败的数据块开始继续上传
  - **服务端支持**：服务端需支持分块上传接口（如 Multipart Upload），支持查询已上传块、续传接口
- **网络状态监听**：
  - **iOS**：使用 Network Framework 或 Reachability 监听网络状态变化
  - **Android**：使用 ConnectivityManager 监听网络连接状态
  - **自动触发**：网络从断开状态恢复为连接状态时，自动检测并恢复待上传任务
- **上传队列管理**：
  - **队列持久化**：上传队列保存到本地数据库，App 重启后仍可恢复
  - **优先级管理**：按上传时间排序，优先恢复最早的上传任务
  - **并发控制**：同时最多 3 个上传任务并发执行（避免占用过多网络资源）
  - **失败重试**：上传失败的数据块自动重试（最多重试 3 次，超过 3 次标记为失败）
- **用户体验优化**：
  - **后台上传**：断点续传在后台执行，不阻塞用户操作
  - **进度通知**：上传进度通过通知栏（Android）或通知中心（iOS）显示
  - **弹窗提示**：网络恢复后自动弹出"正在恢复上传"弹窗，显示恢复进度
  - **静默恢复**：用户关闭弹窗后，上传在后台继续，不影响其他功能使用

#### 4.5.3 推荐生成优化
**推荐方案**：
- 缓存推荐结果（避免重复计算）
- 异步生成推荐（后台计算，不阻塞 UI）
- 支持增量更新（仅更新变化的推荐）

### 4.6 维护机制

#### 4.6.1 人工更新机制
**内容维护**：
- **搭配规则库更新**：
  - 定期（每季度）由专业穿搭顾问团队审核和更新搭配规则
  - 根据时尚趋势、季节变化更新颜色搭配规则库、风格搭配规则库
  - 新增场合适配规则（如新增"面试""年会"等场景）
- **基础搭配模板更新**：
  - 冷启动方案中的基础搭配模板每季度更新一次
  - 根据用户反馈和流行趋势调整模板内容
  - 增加不同风格、不同场合的经典搭配模板
- **豆包 AI 识别优化**：
  - 定期（每月）分析用户修正数据，识别高频错误品类和识别错误模式
  - 针对错误率高的品类优化 Prompt 提示词，提升识别准确率
  - 调整豆包 AI API 调用参数（如 temperature、max_tokens 等），优化识别结果
  - 新增品类识别能力（如新增"连体裤""背带裤"等细分品类）：通过优化 Prompt 和结果后处理实现
  - 建立 Prompt 版本管理机制，支持 A/B 测试不同 Prompt 的效果

**更新流程**：
- 内容团队收集时尚趋势、用户反馈
- 专业顾问审核和制定更新方案
- 技术团队实施更新（规则库、模板、豆包 AI Prompt）
- 灰度测试验证更新效果
- 全量发布更新

#### 4.6.2 用户行为迭代机制
**数据收集**：
- **用户行为数据**：
  - 收藏/应用/不感兴趣的推荐方案
  - "换一件"功能的替换行为（记录替换的单品和替换原因、替换品类、替换次数）
  - 用户实际穿搭记录（匹配推荐方案的使用情况）
  - 用户修正识别结果的频率和类型
  - **上传行为数据**：上传率、上传成功率、批量上传率、上传完成率
  - **推荐行为数据**：推荐生成率、推荐点击率、推荐查看完成率、推荐生成耗时
  - **换一件行为数据**：换一件使用率、替换成功率、平均替换次数、替换品类分布
  - **收藏与应用数据**：推荐收藏率、推荐应用率、收藏后应用率
  - **偏好设置数据**：偏好设置完成率、偏好设置修改率
  - **冷启动数据**：冷启动触发率、冷启动模板查看率、冷启动后上传率
- **用户反馈数据**：
  - 用户对推荐方案的评分（如"很满意""一般""不满意"）
  - 用户主动反馈的搭配问题
  - 用户自定义规则（如"我不喜欢红色+绿色搭配"）

**迭代优化**：
- **推荐算法优化**：
  - **协同过滤优化**：基于用户行为数据（收藏、应用、不感兴趣）持续优化推荐权重
  - **个性化增强**：根据用户历史穿搭记录，学习用户个人搭配偏好
  - **A/B 测试**：定期进行推荐算法 A/B 测试，验证优化效果
- **豆包 AI 识别优化**：
  - **用户修正数据回流**：收集用户修正的识别结果，分析识别错误模式，用于优化 Prompt 和识别策略
  - **错误模式分析**：分析用户修正率高的品类和场景，针对性优化 Prompt 提示词
  - **Prompt 持续优化**：定期（每月）基于用户修正数据优化 Prompt，提升识别准确率
  - **参数调优**：根据识别效果调整豆包 AI API 调用参数，优化识别结果质量
  - **A/B 测试**：定期进行不同 Prompt 版本的 A/B 测试，验证优化效果
- **搭配规则优化**：
  - **用户自定义规则学习**：分析用户自定义规则，发现新的搭配规律
  - **流行趋势学习**：基于用户收藏和应用数据，识别当前流行的搭配趋势
  - **规则权重调整**：根据用户行为数据调整不同规则的权重

**迭代周期**：
- **实时优化**：用户行为数据实时收集和分析
- **每周优化**：推荐算法权重微调
- **每月优化**：豆包 AI Prompt 优化、搭配规则库更新
- **每季度优化**：人工审核和更新内容、模型全面评估

**效果评估**：
- **推荐效果指标**：
  - 推荐方案收藏率（目标：≥30%）
  - 推荐方案应用率（目标：≥20%）
  - 用户满意度评分（目标：≥4.0/5.0）
- **识别效果指标**：
  - 用户修正率（目标：≤10%）
  - 各品类识别准确率（保持或提升）
- **用户活跃度指标**：
  - 日活跃用户数（DAU）
  - 用户留存率（次日留存、7日留存、30日留存）
  - 平均使用时长

---

## 五、用户核心使用流程

### 5.1 首次使用流程

```
1. 打开 App → 欢迎页（产品介绍）
2. **隐私政策弹窗**（必须同意）：
   - 展示隐私政策核心条款（数据存储方式、数据使用范围、数据收集范围）
   - 用户必须勾选"我已阅读并同意隐私政策"
   - 点击"同意并继续"按钮（未勾选时按钮不可点击）
   - 未同意前，核心功能（上传、推荐等）不可用
3. 注册/登录（支持手机号、微信、Apple ID）
4. 用户偏好设置引导（3-4 步）：
   - 选择喜欢的风格（多选）
   - 选择常去场合（多选）
   - 输入身高体型参考（可选）
   - 选择颜色偏好（可选）
5. 进入首页 → 提示"开始上传衣物建立衣橱"
6. 点击"上传衣物" → 选择照片 → AI 识别 → 查看/编辑结果 → 保存
7. 重复上传，建立基础衣橱（建议至少 10 件）
8. 点击"生成推荐" → 选择场合和天气 → 查看推荐方案
```

**预计耗时**：首次完整流程 5-10 分钟

---

### 5.2 日常使用流程（推荐场景）

#### 场景 A：每日出门前查看推荐
**方式一：通过推送通知（推送权限开启时）**
```
1. 早上 7:00 收到推送通知（如已设置）
2. 点击通知 → 进入推荐详情页
3. 查看 3-5 套推荐方案，阅读搭配亮点
4. 选择喜欢的方案 → 点击"应用"或"收藏"
5. 如需调整 → 点击单品 → "换一件" → 选择替换
6. 确定最终方案 → 开始穿搭
```

**方式二：通过首页"每日推荐"入口（推送权限关闭时）**
```
1. 打开 App → 进入首页
2. 查看首页顶部"每日推荐"入口卡片
3. 点击"每日推荐" → 自动生成当日推荐方案（使用默认场合，自动获取当日天气）
4. 查看 3-5 套推荐方案，阅读搭配亮点
5. 选择喜欢的方案 → 点击"应用"或"收藏"
6. 如需调整 → 点击单品 → "换一件" → 选择替换
7. 确定最终方案 → 开始穿搭
```

**预计耗时**：2-3 分钟

#### 场景 B：手动生成推荐
**方式一：快捷入口（推荐）**
```
1. 打开 App → 首页
2. 点击首页预设的场合快捷按钮（如"通勤""约会""运动"）
3. 系统自动获取当日天气，立即生成推荐方案（3-5 套）
4. 浏览搭配亮点，选择喜欢的方案
5. 如需调整 → "换一件"功能
6. 收藏或应用方案
```

**方式二：完整选择**
```
1. 打开 App → 首页
2. 点击"生成推荐"按钮
3. 选择场合（如"职场通勤"）和天气（如"晴天"）
4. 查看推荐方案（3-5 套）
5. 浏览搭配亮点，选择喜欢的方案
6. 如需调整 → "换一件"功能
7. 收藏或应用方案
```

**预计耗时**：
- 快捷入口：30 秒 - 1 分钟
- 完整选择：1-2 分钟

#### 场景 C：添加新衣物
```
1. 打开 App → 进入"我的衣橱"
2. 点击"+"按钮 → 选择"上传衣物"
3. 选择照片（单张或批量）→ 确认上传
4. 等待 AI 识别（显示进度）
5. 查看识别结果 → 如有错误，点击编辑
6. 添加自定义标签（如"新衣服""显瘦"）
7. 保存 → 返回衣橱列表
```

**预计耗时**：30 秒 - 1 分钟/件

#### 场景 F：管理已淘汰衣物
```
1. 打开 App → 进入"我的衣橱"
2. 点击筛选栏中的"已淘汰"快捷按钮
3. 查看所有已淘汰的衣物（显示已淘汰数量，如"已淘汰(5)"）
4. 选择操作方式：
   a. 批量恢复：选择多个已淘汰衣物 → 点击"批量恢复" → 确认恢复
   b. 批量删除：选择多个已淘汰衣物 → 点击"批量删除" → 二次确认删除
   c. 单个操作：点击单个衣物卡片 → 选择"恢复"或"删除"
5. 操作完成后，显示成功提示，自动更新筛选结果
```

**预计耗时**：30 秒 - 1 分钟（取决于操作数量）

#### 场景 D：记录今日穿搭
```
1. 打开 App → 进入"穿搭记录"
2. 点击"记录今日穿搭"
3. 上传穿搭照片（最多 3 张）
4. 标记搭配的单品（从衣橱中选择）
5. 添加文字记录（心情、场合等）
6. 添加标签（如"今日最佳"）
7. 保存 → 生成穿搭日记
```

**预计耗时**：1-2 分钟

#### 场景 E：冷启动方案（衣橱单品不足时）
**触发条件**：用户衣橱单品数量 < 10 件，或缺少关键品类（如无下装、无鞋子等）

**流程**：
```
1. 用户点击"生成推荐" → 系统检测衣橱状态
2. 如单品不足，显示冷启动引导页面：
   a. 展示"基础搭配模板"（如"白T+牛仔裤+小白鞋"等经典搭配）
   b. 标注"参考模板，非您衣橱单品"（避免用户误解）
   c. 显示"补齐衣橱"按钮，引导用户上传缺失品类
3. 智能提示缺失品类：
   - 如缺少下装 → 提示"您缺少下装，建议上传裤子或裙子"
   - 如缺少鞋子 → 提示"您缺少鞋子，建议上传至少 1 双"
   - 如缺少上衣 → 提示"您缺少上衣，建议上传至少 2 件"
4. 用户可选择：
   a. 立即上传缺失品类 → 跳转到上传页面
   b. 查看基础搭配模板 → 浏览更多经典搭配方案
   c. 临时"虚拟试穿"外部单品 → 为 Phase 3 购物功能埋点
5. "虚拟试穿"功能（Phase 3 埋点）：
   - 用户可从推荐的商品链接中选择单品
   - 将外部单品临时加入"虚拟衣橱"（标记为"未购买"）
   - 基于虚拟衣橱生成搭配方案（标注"包含未购买单品"）
   - 用户可查看完整搭配效果，决定是否购买
```

**交互设计**：
- 冷启动页面采用友好的引导式设计，避免用户因单品不足而流失
- 基础搭配模板以卡片形式展示，每套模板包含单品图片和搭配说明
- 缺失品类提示采用醒目的卡片样式，支持一键跳转上传
- "虚拟试穿"功能入口明显，但标注"即将推出"或"Beta 功能"

**预计耗时**：2-5 分钟（取决于用户选择补齐衣橱还是查看模板）

---

### 5.3 关键交互节点

#### 节点 1：上传识别
- **用户操作**：选择照片 → 确认上传
- **系统反馈**：显示上传进度 → 显示识别进度 → 展示识别结果
- **容错处理**：
  - **识别失败**：
  - 网络错误：提示"网络连接失败，请检查网络后重试"
  - 识别服务错误：提示"识别服务暂时不可用，请稍后重试"或"手动输入"
  - 照片质量问题：提示"照片模糊或无法识别，请重新拍摄清晰照片"
  - **网络中断**：照片保存在本地队列，显示"等待上传"状态，网络恢复后自动继续上传
  - **同步冲突**：检测到多设备编辑冲突时，提示用户选择保留的版本

#### 节点 2：查看推荐
- **用户操作**：选择场合和天气 → 查看推荐
- **系统反馈**：显示加载动画 → 展示推荐卡片 → 标注搭配亮点
- **容错处理**：
  - **衣橱单品充足**：正常生成推荐方案
  - **衣橱单品不足**：触发冷启动方案
    - 展示基础搭配模板（标注"参考模板"）
    - 智能提示缺失品类，引导用户补齐
    - 提供"虚拟试穿"入口（Phase 3 埋点）
  - **全部衣物已淘汰**：提示"您的衣橱中没有可用衣物，请先取消部分衣物的'已淘汰'标记"，提供快捷入口跳转到衣橱管理

#### 节点 3：换一件
- **用户操作**：点击单品 → 选择替换 → 确认
- **系统反馈**：
  - 弹出替换选择器 → AI 重新评估整体方案（校验风格/颜色一致性）→ 展示新搭配 → 动画过渡
  - 显示"AI 正在重新评估搭配..."提示，完成后展示优化后的新搭配
- **容错处理**：无同品类衣物时提示"暂无其他可选单品"

---

## 六、界面设计规范

### 6.1 设计风格
- **整体风格**：简约清新，现代感强
- **主色调**：莫兰迪色系（低饱和度、高级感）
  - 主色：灰蓝色（#6B8E9F）
  - 辅助色：米白色（#F5F5F5）、浅灰色（#E8E8E8）
  - 强调色：珊瑚粉（#FF8C94）、薄荷绿（#A8E6CF）
  - 文字颜色：主文字（#333333）、次要文字（#666666）、提示文字（#999999）
  - 背景色：主背景（#FFFFFF）、次背景（#F5F5F5）、分割线（#E8E8E8）
- **字体**：系统默认字体（iOS：SF Pro，Android：Roboto）
  - **主字体大小规范**：
    - **标题**：16sp/16pt（页面标题、卡片标题）
    - **正文**：14sp/14pt（主要文字内容）
    - **辅助文字**：12sp/12pt（次要信息、提示文字、标签、时间戳）
  - **扩展字体大小**（特殊场景）：
    - 大标题：20sp/20pt（特殊页面标题，如欢迎页）
    - 中标题：18sp/18pt（重要卡片标题）
    - 小字：14sp/14pt（次要信息，与正文相同）
    - 极小字：12sp/12pt（标签、时间戳，与辅助文字相同）
  - **行间距**：1.2 倍行高（14sp 正文行高 16.8sp）
  - **字重**：常规（Regular 400）、中等（Medium 500）、粗体（Bold 700）
- **图标**：线性图标风格，统一粗细（2px）
- **圆角规范**：
  - **卡片圆角**：8px（主要卡片，如推荐卡片、衣物卡片、信息卡片）
  - 按钮圆角：6px（主要按钮）、4px（次要按钮）
  - 图片圆角：4px（缩略图）、8px（大图）
  - 输入框圆角：4px
- **阴影规范**：
  - **卡片阴影**：opacity 0.1（所有卡片统一使用，包括推荐卡片、衣物卡片、信息卡片）
  - **阴影参数**：
    - iOS：shadowColor: rgba(0, 0, 0, 0.1), shadowOffset: (0, 2), shadowRadius: 4, shadowOpacity: 0.1
    - Android：elevation: 2dp, shadowColor: rgba(0, 0, 0, 0.1)
  - **应用场景**：所有卡片元素统一使用，提升层次感和视觉深度
- **间距规范**：
  - 页面边距：16px（左右边距）
  - 卡片间距：12px（垂直间距）、16px（水平间距）
  - 内容内边距：16px（卡片内边距）、12px（列表项内边距）
  - 元素间距：8px（小间距）、12px（中间距）、16px（大间距）

### 6.2 核心页面布局

#### 首页
- **顶部**：
  - 天气信息卡片（温度、天气状况、定位城市）
  - **"每日推荐"入口**（条件显示）：
    - **显示条件**：用户关闭推送权限时显示
    - **位置**：天气信息卡片下方
    - **样式**：卡片样式，与天气信息卡片风格一致，显示"今日推荐"文字和推荐图标
    - **功能**：点击后自动生成当日推荐方案（使用默认场合或用户最近选择的场合，自动获取当日天气）
    - **隐藏条件**：用户开启推送权限时自动隐藏
- **中部**：
  - **场合快速选择**：预设常用场合快捷按钮（如"通勤""约会""运动"），用户一键选择即可生成推荐
  - **快速生成推荐入口**：大按钮，点击后进入完整的选择页面（场合+天气）
- **下部**：最近推荐方案（横向滑动卡片）

#### 衣橱页
- **顶部**：
  - 筛选栏（横向滑动，品类/风格/季节/标签）
  - **"已淘汰"快捷筛选按钮**：筛选栏中突出显示，显示已淘汰衣物数量（如"已淘汰(5)"）
- **中部**：衣物网格（2 列或 3 列，用户可选）
  - **已淘汰状态显示**：已淘汰的衣物卡片显示"已淘汰"标签或灰色遮罩，便于区分
- **底部**：上传按钮（悬浮按钮，固定在右下角）
- **已淘汰筛选状态**：
  - 点击"已淘汰"按钮后，筛选栏高亮显示，仅显示已淘汰的衣物
  - 支持在已淘汰状态下继续使用其他筛选条件
  - 支持批量恢复或批量删除操作

#### 推荐页
- **顶部**：场合和天气选择器（下拉选择）
- **中部**：推荐方案卡片（纵向滚动，每套一张卡片）
- **卡片内容**：单品图片（横向滑动）、搭配亮点文字、操作按钮（收藏/换一件）

### 6.3 交互原则
- **三步内完成核心操作**：上传 → 识别 → 推荐
- **减少层级**：核心功能不超过 3 层导航
- **即时反馈**：所有操作都有明确的视觉反馈（加载、成功、失败）
- **容错设计**：识别失败、网络错误等场景有友好的提示和解决方案

#### 6.3.1 交互动效规范
**动画时长**：
- **页面跳转**：300ms（标准缓动曲线 ease-in-out）
- **卡片滑动**：300ms（横向滑动，如推荐卡片、衣物卡片）
- **替换单品过渡动画**：200ms（"换一件"功能中，替换单品后的过渡动画，使用 ease-in-out 缓动曲线）
- **弹窗出现/消失**：250ms（弹窗、底部选择器）
- **按钮点击反馈**：150ms（按钮按下效果）
- **加载动画**：循环动画，循环周期 1.5s（加载动画的完整循环周期，确保动画流畅自然）
- **列表项出现**：200ms（列表滚动时的渐入效果）

**动画曲线**：
- **标准缓动**：ease-in-out（页面跳转、卡片滑动）
- **快速进入**：ease-out（弹窗出现）
- **快速退出**：ease-in（弹窗消失）
- **弹性效果**：spring（iOS 风格，用于特殊交互，如下拉刷新）

**交互反馈**：
- **按钮点击**：按下时透明度 0.7，松开恢复，时长 150ms
- **列表项点击**：按下时背景色变化（浅灰色），松开恢复，时长 200ms
- **长按操作**：长按 500ms 后触发（如衣物卡片长按进入编辑）
- **滑动操作**：支持惯性滑动，滑动距离超过阈值触发操作（如左滑删除）

#### 6.3.2 平板横屏布局优化
**衣橱页面**：
- **筛选栏布局**：
  - **竖屏模式**：顶部横向滑动筛选栏（保持手机布局）
  - **横屏模式**：左侧固定侧边栏（宽度 200px），筛选选项垂直排列
  - **优势**：横屏时充分利用屏幕宽度，筛选选项更易查看和操作
- **网格布局**：
  - **竖屏模式**：3-4 列网格（根据屏幕宽度自适应）
  - **横屏模式**：5-6 列网格（充分利用横向空间）
- **详情展示**：横屏时支持左右分栏（左侧列表，右侧详情）

**推荐页面**：
- **竖屏模式**：单栏列表，推荐卡片垂直排列
- **横屏模式**：2 列网格布局，推荐卡片并排显示
- **详情展示**：横屏时点击卡片，右侧显示详情（不跳转新页面）

**首页**：
- **竖屏模式**：天气卡片 + 快捷按钮 + 最近推荐（垂直排列）
- **横屏模式**：左右分栏（左侧：天气卡片 + 快捷按钮，右侧：最近推荐）

**通用适配规则**：
- **最小触摸区域**：44×44pt（iOS）/ 48×48dp（Android），确保触摸准确性
- **字体缩放**：平板设备字体自动放大 10-15%，提升可读性
- **间距调整**：平板设备间距适当增大（边距 24px，卡片间距 16px）

---

## 七、非功能需求补充

### 7.1 平台适配
- **iOS**：支持 iOS 13.0 及以上版本
- **Android**：支持 Android 8.0（API 26）及以上版本
- **平板适配**：支持 iPad 和 Android 平板，横屏显示优化（多列布局）

#### 7.1.1 不同机型适配规则
**折叠屏设备适配**：
- **展开状态（大屏模式）**：
  - 衣橱页面：3-4 列网格布局（充分利用大屏空间）
  - 推荐页面：左右分栏布局（左侧推荐列表，右侧详情预览）
  - 穿搭记录：时间轴 + 详情并排显示
  - 字体和图标：适当放大，适配大屏观看
- **折叠状态（普通手机模式）**：
  - 保持与普通手机相同的布局（2 列网格、单栏列表）
  - 自动检测折叠状态变化，动态调整布局
- **折叠屏特殊交互**：
  - 支持拖拽操作（如从衣橱拖拽单品到推荐方案）
  - 支持分屏多任务（如一边查看推荐，一边查看衣橱）
  - 折叠/展开时平滑过渡动画

**不同分辨率适配规则**：
- **小屏设备（< 5.5 英寸，如 iPhone SE）**：
  - 衣橱页面：2 列网格布局
  - 字体大小：最小 14px，确保可读性
  - 按钮尺寸：最小 44×44pt（符合触摸标准）
  - 卡片间距：适当缩小，节省空间
- **中屏设备（5.5-6.5 英寸，主流手机）**：
  - 衣橱页面：2-3 列网格布局（用户可选）
  - 字体大小：标准 16px
  - 卡片间距：标准间距
- **大屏设备（> 6.5 英寸，如 iPhone Pro Max）**：
  - 衣橱页面：3 列网格布局
  - 字体大小：可适当放大至 18px
  - 充分利用大屏空间，展示更多信息
- **平板设备（iPad、Android 平板）**：
  - 横屏模式：4-5 列网格布局
  - 竖屏模式：3-4 列网格布局
  - 推荐页面：左右分栏或网格布局
  - 支持多窗口操作

**响应式布局原则**：
- 使用相对单位（dp/pt、百分比）而非绝对像素
- 支持动态字体大小（跟随系统设置）
- 图片自适应缩放，保持宽高比
- 关键元素（按钮、输入框）最小触摸区域 44×44pt
- 测试覆盖主流设备分辨率：
  - iOS：iPhone SE、iPhone 12/13/14、iPhone Pro Max、iPad
  - Android：1080×1920、1440×2560、2160×3840 等主流分辨率

### 7.2 性能要求
- **启动时间**：
  - **首次安装启动**：< 5 秒（P95，包含首次安装后的初始化、数据库创建、资源加载等）
  - **常规冷启动**：< 3 秒（P95，非首次安装的冷启动，包含 App 启动、数据加载、界面渲染等）
  - **热启动**：< 1 秒（P95，App 在后台时的启动）
  - **统计方式**：从用户点击 App 图标到首页完全渲染可交互的时间
- **识别速度**：
  - **单张照片识别**：< 8 秒（P95，包含上传时间 + 云端识别时间 + 结果返回时间）
  - **批量识别（9张）**：< 30 秒（P95，并行上传和识别）
  - **网络优化**：支持识别结果缓存，相同照片无需重复识别
- **推荐生成**：推荐生成 < 3 秒（P95）
- **图片加载**：列表图片加载 < 1 秒（P95）
- **页面流畅度**：
  - **卡顿判定标准**：页面滑动 FPS < 45 持续 1 秒以上判定为卡顿
  - **目标值**：
    - 页面滑动 FPS ≥ 50（P95，60Hz 屏幕）
    - 页面滑动 FPS ≥ 30（P95，低端设备）
    - 卡顿率 < 5%（页面滑动过程中出现卡顿的比例）
  - **统计方式**：实时监控页面滑动时的 FPS，记录 FPS < 45 且持续 1 秒以上的次数

### 7.3 内容策略
- **无强制广告**：主流程无广告干扰
- **可选推送**：仅允许可选的穿搭技巧干货推送（用户可关闭）
- **内容质量**：推送内容需有价值，避免营销性质

### 7.4 数据安全
- **加密传输**：所有网络请求使用 HTTPS
- **本地加密**：敏感数据本地加密存储
- **API 密钥安全**：
  - **豆包 AI API Key 安全存储**：API Key 存储在设备本地加密存储，不硬编码在代码中
  - **密钥轮换机制**：支持 API Key 动态更新，无需 App 更新即可更换密钥
  - **密钥权限控制**：API Key 仅用于调用豆包 AI 识别接口，最小权限原则
- **权限最小化**：仅申请必要权限（相机、相册、位置、日历），明确告知使用目的

#### 7.4.1 权限申请粒度说明
**位置权限**：
- **申请时机**：首次需要获取天气信息时申请（如用户点击"生成推荐"或查看首页天气）
- **授权选项**：
  - **iOS**：
    - 支持"仅在使用时授权"（When In Use）：App 使用期间获取位置
    - 支持"单次授权"（One-Time）：iOS 14+ 支持，仅本次使用获取位置
    - 不支持"始终授权"（Always）：不进行后台定位
  - **Android**：
    - 支持"仅在使用时授权"（Fine Location）：App 使用期间获取位置
    - 支持"单次授权"（One-Time）：Android 11+ 支持，仅本次使用获取位置
    - 不支持"始终授权"（Background Location）：不进行后台定位
  - **说明**：仅在使用 App 时获取位置信息，用于获取天气数据，不进行后台定位
- **拒绝处理**：用户拒绝位置权限时，支持手动选择城市获取天气
- **权限说明文案**："衣搭库需要获取您的位置信息，用于自动获取当地天气，为您推荐合适的穿搭方案"

**日历权限**（Phase 3 可选功能）：
- **申请时机**：用户主动开启"日程读取"功能时申请
- **授权选项**：
  - **iOS**：使用 EventKit 框架，支持读取日历权限
  - **Android**：使用 CalendarContract，支持读取日历权限
- **读取范围设置**（用户可在设置中选择）：
  - **读取全部**：读取所有日历日程（包括过去和未来）
  - **未来 7 天**：仅读取未来 7 天内的日程（默认选项，降低隐私风险）
  - **自定义时间段**：用户可自定义读取范围（如未来 3 天、未来 14 天、未来 30 天等）
  - **读取内容**：仅读取日程标题和描述，用于识别场合关键词，不读取其他信息（如参与者、位置详情等）
- **设置入口**：用户可在"设置-日程读取"中选择读取范围，随时调整
- **拒绝处理**：用户拒绝日历权限时，功能自动关闭，不影响其他功能使用
- **权限说明文案**："衣搭库需要读取您的日历日程，用于识别场合信息（如会议、约会），自动为您推荐合适的穿搭方案。您可在设置中选择读取范围（默认仅读取未来 7 天）"

**相机权限**：
- **申请时机**：用户点击"上传衣物"或"拍照"时申请
- **授权选项**：支持"仅在使用时授权"
- **权限说明文案**："衣搭库需要访问相机，用于拍摄衣物照片并识别"

**相册权限**：
- **申请时机**：用户选择"从相册选择"时申请
- **授权选项**：
  - **iOS 14+**：支持"选择照片"（Limited Photo Library），用户可选择部分照片授权
  - **Android**：支持"仅在使用时授权"
- **权限说明文案**："衣搭库需要访问相册，用于选择衣物照片并识别"

#### 7.4.2 数据删除与导出规则
**数据删除规则**：
- **用户主动删除**：
  - **本地数据**：用户删除衣物/穿搭记录时，立即从本地数据库删除
  - **本地缓存**：删除相关衣物时，立即清除该衣物的所有缓存（缩略图、识别结果、临时文件等）
  - **云端备份**：
    - **默认策略**：为支持数据恢复功能，已删除数据在云端保留 7 天（标记为"已删除"状态，用户不可见）
    - **7 天内可恢复**：用户删除数据后 7 天内，可在"设置-数据恢复"中恢复误删数据
    - **7 天后自动清除**：7 天后从云端彻底删除，无法恢复
    - **永久删除选项**：用户可在删除时选择"永久删除"，立即从云端彻底删除，无法恢复（需二次确认）
    - **永久删除设置**：用户可在"设置-数据管理"中设置默认删除策略（7 天可恢复/永久删除）
- **账户注销**：
  - **立即清除**：用户注销账户时，所有本地数据立即清除
  - **云端清除**：账户注销后 24 小时内清除所有云端数据（包括备份、行为数据等）
  - **匿名化处理**：行为统计数据匿名化处理，保留用于产品分析（符合 GDPR 要求）
- **数据恢复**：
  - **恢复窗口**：用户删除数据后 7 天内（默认策略），可在"设置-数据恢复"中恢复误删数据
  - **恢复范围**：支持恢复衣物、穿搭记录、收藏等数据（仅限使用默认删除策略的数据，永久删除的数据无法恢复）
  - **恢复后状态**：恢复的数据保持原有状态（标签、备注等），同时恢复云端备份
  - **恢复限制**：永久删除的数据无法恢复，用户需谨慎选择

**数据导出功能**：
- **导出内容**：
  - 衣橱数据：所有衣物的照片、识别结果、标签、备注
  - 穿搭记录：所有穿搭日记（照片、文字记录、标签）
  - 收藏数据：所有收藏的穿搭方案
  - 用户偏好：风格偏好、场合偏好、颜色偏好等设置
- **导出格式**（用户可选择）：
  - **JSON 格式**：仅导出结构化数据（JSON 文件），包含所有衣物的识别结果、标签、备注、穿搭记录、收藏、用户偏好等
  - **图片压缩包**：仅导出照片（ZIP 压缩包），包含所有衣物的原始照片和穿搭记录照片
  - **完整导出**：JSON + 图片压缩包（ZIP 压缩包，包含照片文件夹和数据 JSON 文件）
- **导出方式**：
  - **本地导出**：保存到设备本地（相册/文件管理器）
  - **云端导出**：通过邮件或云盘分享链接（链接有效期 7 天）
- **导出入口**：设置页面 → "数据导出" → 选择导出内容 → 选择导出格式（JSON/图片压缩包/完整导出）→ 确认导出
- **导出限制**：每月最多导出 3 次，防止滥用

#### 7.4.3 隐私政策与用户协议
**隐私政策展示时机**：
- **首次启动**：用户首次打开 App 时，在欢迎页后、注册/登录前，弹出隐私政策弹窗
- **弹窗内容**：
  - **核心条款展示**：
    - **数据存储方式**：明确说明数据存储位置（本地存储/云端备份），加密方式
    - **数据使用范围**：明确说明数据用途（AI 识别、推荐生成、算法优化），不用于广告投放、不出售给第三方
    - **数据收集范围**：简要说明收集的数据类型（照片、偏好、行为数据等）
  - **用户同意机制**：
    - 提供勾选框"我已阅读并同意隐私政策"（必须勾选）
    - "同意并继续"按钮（勾选后才能点击，点击后进入 App）
    - "查看完整隐私政策"链接（跳转到完整政策页面，用户可随时查看）
  - **拒绝处理**：用户未勾选同意时，无法使用核心功能（上传、推荐等），仅可查看完整隐私政策
- **核心功能限制**：用户勾选同意前，核心功能（上传衣物、生成推荐等）不可用，仅可浏览 App 介绍和查看隐私政策
- **更新提醒**：隐私政策更新时，用户下次打开 App 时弹出更新提醒，需重新勾选同意
- **随时查看**：用户可在"设置-隐私政策"中随时查看完整隐私政策

**隐私政策核心条款**：
- **数据收集范围**：
  - 用户上传的衣物照片（通过 API 调用豆包 AI 进行识别，照片仅用于识别，不存储到豆包 AI 服务器，识别完成后可选择删除或保留在云端）
  - 用户偏好设置（风格、场合、颜色偏好等）
  - 用户行为数据（收藏、应用、不感兴趣等，匿名化处理）
  - 设备信息（设备型号、系统版本，用于适配和性能优化）
  - 位置信息（仅在使用时获取，用于天气服务）
  - 日历信息（仅当用户开启"日程读取"功能时，仅读取未来 7 天内的日程标题和描述）
- **数据使用范围**：
  - **核心功能**：用于 AI 识别（通过豆包 AI API）、推荐生成、个性化服务
  - **第三方服务**：照片通过 API 调用豆包 AI 进行识别，豆包 AI 仅用于识别处理，不存储用户照片
  - **算法优化**：匿名化行为数据用于优化推荐算法
  - **产品改进**：匿名化数据用于分析用户需求，改进产品功能
  - **不用于**：不用于广告投放、不向第三方出售用户数据、不用于其他商业目的
- **数据存储方式**：
  - **第三方 AI 识别**：用户照片通过 API 调用豆包 AI 进行识别（加密传输，照片仅用于识别，不存储到豆包 AI 服务器）
  - **照片存储策略**：用户可选择"仅识别模式"（识别后删除照片）或"云端备份模式"（保留照片在云端）
  - **本地存储**：识别结果、标签、备注等存储在设备本地（加密存储）
  - **云端同步**：用户可选择开启云端备份，同步识别结果和标签（照片根据存储策略决定是否同步）
  - **数据保留**：用户删除数据后 7 天内可恢复，7 天后彻底清除
- **数据共享**：不与第三方共享用户个人数据，仅与天气服务提供商共享位置信息（用于获取天气）

**用户协议核心条款**：
- **服务内容**：明确说明 App 提供的服务内容（电子衣橱管理、AI 推荐等）
- **用户权利**：
  - 用户有权随时删除自己的数据
  - 用户有权导出自己的数据
  - 用户有权拒绝非必要权限
- **用户义务**：
  - 用户需保证上传的照片为本人所有或已获得授权
  - 用户需遵守相关法律法规，不得上传违法违规内容
- **服务变更**：公司有权根据业务需要调整服务内容，但需提前通知用户
- **免责声明**：AI 识别和推荐仅供参考，不保证 100% 准确，用户需自行判断

### 7.5 数据统计与分析需求

#### 7.5.1 核心行为统计指标
**统计目的**：通过核心行为数据监控产品健康度，识别用户痛点，支撑产品迭代和算法优化。

**1. 上传相关统计**：
- **上传次数**：
  - 定义：用户上传照片的总次数（按次统计，每次上传操作计为 1 次）
  - 统计维度：按日、周、月、按用户统计
  - 埋点事件：`event_upload_start`（用户点击上传按钮时触发）
  - 用途：监控用户上传行为频率，分析上传使用模式
- **上传成功率**：
  - 定义：成功上传并完成识别的照片数 / 用户选择上传的照片总数
  - 统计维度：按上传批次、按用户统计
  - 目标值：≥95%
  - 埋点事件：`event_upload_success`（上传成功时触发）、`event_upload_fail`（上传失败时触发）
  - 用途：识别网络问题、识别失败等技术问题
- **上传率**：
  - 定义：每日有上传行为的用户数 / 日活跃用户数（DAU）
  - 统计维度：按日、周、月统计
  - 目标值：≥15%（新用户前7天），≥5%（老用户）
  - 用途：监控用户建立衣橱的积极性，识别上传流程问题
- **批量上传率**：
  - 定义：使用批量上传（≥3张）的用户数 / 总上传用户数
  - 统计维度：按用户统计
  - 目标值：≥40%
  - 用途：评估批量上传功能的接受度
- **上传完成率**：
  - 定义：完成上传并保存到衣橱的衣物数 / 开始上传的衣物数
  - 统计维度：按上传批次统计
  - 目标值：≥90%
  - 用途：识别上传流程中的流失点（如识别后未保存）

**2. 推荐相关统计**：
- **推荐查看次数**：
  - 定义：用户查看推荐方案的总次数（每次进入推荐详情页计为 1 次）
  - 统计维度：按日、周、月、按用户统计
  - 埋点事件：`event_recommend_view`（用户进入推荐详情页时触发）
  - 用途：监控推荐功能使用频率，分析用户查看推荐的行为模式
- **推荐收藏率**：
  - 定义：收藏推荐方案的次数 / 查看推荐方案的总次数
  - 统计维度：按推荐来源、按用户统计
  - 目标值：≥30%
  - 埋点事件：`event_recommend_favorite`（用户收藏推荐时触发）
  - 用途：评估推荐方案质量，优化推荐算法
- **推荐生成率**：
  - 定义：成功生成推荐方案的请求数 / 用户请求生成推荐的总次数
  - 统计维度：按日、按用户统计
  - 目标值：≥98%（衣橱充足时）
  - 用途：监控推荐功能可用性
- **推荐点击率**：
  - 定义：点击查看推荐详情的用户数 / 收到推荐通知或生成推荐的用户数
  - 统计维度：按推荐来源（推送/手动/快捷入口/每日推荐入口）统计
  - 目标值：≥60%（推送推荐），≥80%（手动生成），≥70%（每日推荐入口）
  - 用途：评估推荐内容吸引力，优化推荐时机和方式
- **"每日推荐"入口使用率**：
  - 定义：点击首页"每日推荐"入口的用户数 / 推送权限关闭的用户数
  - 统计维度：按日、按用户统计
  - 目标值：≥50%（推送权限关闭用户中）
  - 用途：评估推送权限关闭时的降级方案有效性，确保用户仍能方便获取每日推荐
- **推送权限关闭率**：
  - 定义：关闭推送权限的用户数 / 总用户数
  - 统计维度：按用户统计
  - 用途：监控推送权限接受度，优化推送功能和"每日推荐"入口
- **推荐查看完成率**：
  - 定义：完整浏览所有推荐方案（滑动到底部）的用户数 / 点击查看推荐的用户数
  - 统计维度：按推荐来源统计
  - 目标值：≥50%
  - 用途：评估推荐方案质量和数量是否合适
- **推荐生成耗时**：
  - 定义：从用户点击"生成推荐"到显示推荐方案的平均耗时
  - 统计维度：按设备性能、网络状况统计
  - 目标值：< 3 秒（P95）
  - 用途：监控推荐生成性能，优化算法效率

**3. "换一件"功能统计**：
- **换一件使用次数**：
  - 定义：用户使用"换一件"功能的总次数（每次点击"换一件"按钮计为 1 次）
  - 统计维度：按日、周、月、按用户统计
  - 埋点事件：`event_replace_item_click`（用户点击"换一件"按钮时触发）
  - 用途：监控"换一件"功能使用频率，分析用户对推荐方案的满意度
- **换一件使用率**：
  - 定义：使用"换一件"功能的用户数 / 查看推荐的用户数
  - 统计维度：按日、按用户统计
  - 目标值：≥30%
  - 用途：评估推荐方案满意度，识别需要优化的推荐场景
- **换一件替换成功率**：
  - 定义：完成替换并确认新方案的次数 / 点击"换一件"的总次数
  - 统计维度：按品类（上衣/下装/鞋子/配饰）统计
  - 目标值：≥70%
  - 用途：识别替换流程问题，评估同品类衣物数量是否充足
- **换一件平均替换次数**：
  - 定义：用户对同一套推荐方案的平均替换次数
  - 统计维度：按用户、按推荐场景统计
  - 目标值：1-2 次
  - 用途：评估推荐方案初始质量，过多替换说明推荐不准确
- **换一件替换品类分布**：
  - 定义：各品类被替换的比例（上衣/下装/鞋子/配饰）
  - 统计维度：按品类统计
  - 用途：识别推荐算法在哪些品类上表现不佳，针对性优化

**4. 收藏与应用统计**：
- **推荐收藏率**：
  - 定义：收藏推荐方案的次数 / 查看推荐方案的总次数
  - 统计维度：按推荐来源、按用户统计
  - 目标值：≥30%
  - 用途：评估推荐方案质量，优化推荐算法
- **推荐应用率**：
  - 定义：点击"应用"或标记"今日穿搭"的次数 / 查看推荐方案的总次数
  - 统计维度：按推荐来源、按用户统计
  - 目标值：≥20%
  - 用途：评估推荐方案的实际使用价值
- **收藏后应用率**：
  - 定义：从收藏中应用的次数 / 收藏的总次数
  - 统计维度：按时间间隔（当日/3日内/7日内）统计
  - 目标值：≥40%（7日内）
  - 用途：评估收藏功能的价值，优化收藏管理功能

**5. 用户偏好设置统计**：
- **偏好设置完成率**：
  - 定义：完成全部偏好设置的用户数 / 注册用户数
  - 统计维度：按设置步骤统计
  - 目标值：≥70%
  - 用途：识别设置流程中的流失点
- **偏好设置修改率**：
  - 定义：修改过偏好设置的用户数 / 完成初始设置的用户数
  - 统计维度：按修改频率统计
  - 用途：评估偏好设置的准确性，优化初始引导流程

**6. 冷启动方案统计**：
- **冷启动触发率**：
  - 定义：触发冷启动方案的用户数 / 尝试生成推荐的用户数
  - 统计维度：按用户注册时长统计
  - 用途：监控新用户衣橱建立情况
- **冷启动模板查看率**：
  - 定义：查看基础搭配模板的用户数 / 触发冷启动的用户数
  - 统计维度：按模板类型统计
  - 目标值：≥60%
  - 用途：评估冷启动方案的有效性
- **冷启动后上传率**：
  - 定义：冷启动后7日内上传衣物的用户数 / 触发冷启动的用户数
  - 统计维度：按时间间隔统计
  - 目标值：≥50%
  - 用途：评估冷启动引导效果

**7. 离线使用统计**：
- **离线使用时长**：
  - 定义：用户在离线状态下使用 App 的总时长（分钟）
  - 统计维度：按日、周、月、按用户统计
  - 埋点事件：`event_offline_start`（检测到离线状态时触发）、`event_offline_end`（恢复在线时触发）
  - 用途：评估离线功能的使用价值，优化离线体验
- **离线功能使用率**：
  - 定义：使用离线功能的用户数 / 总用户数
  - 统计维度：按用户统计
  - 用途：监控离线功能的接受度，识别离线使用场景

#### 7.5.2 数据统计实现要求
**数据埋点要求**：
- **全流程埋点**：覆盖用户从注册到使用的全流程关键节点
- **事件命名规范**：采用统一的命名规范（如：`event_upload_start`、`event_recommend_view`、`event_replace_item_click`）
- **核心行为埋点事件**：
  - **上传相关**：`event_upload_start`（上传开始）、`event_upload_success`（上传成功）、`event_upload_fail`（上传失败）
  - **推荐相关**：`event_recommend_view`（查看推荐）、`event_recommend_favorite`（收藏推荐）
  - **换一件相关**：`event_replace_item_click`（点击换一件）
  - **离线相关**：`event_offline_start`（离线开始）、`event_offline_end`（离线结束）
- **参数标准化**：关键事件包含标准参数（用户ID、时间戳、设备信息、网络状况、FPS 等）
- **实时上报**：关键行为数据实时上报，非关键数据批量上报（降低性能影响）
- **性能埋点**：实时监控 FPS、启动时间等性能指标，FPS < 45 持续 1 秒以上时触发卡顿事件上报

**统计维度要求**：
- **时间维度**：按日、周、月、季度统计
- **用户维度**：按新用户（注册7天内）、老用户、活跃用户、流失用户统计
- **功能维度**：按功能模块（上传、推荐、换一件、收藏等）统计
- **场景维度**：按推荐场景（通勤、约会、运动等）统计

**数据看板要求**：
- **实时监控看板**：展示核心指标实时数据（上传率、推荐点击率、换一件使用率等）
- **趋势分析看板**：展示核心指标的时间趋势（日/周/月趋势）
- **异常告警机制**：核心指标异常时（如上传率下降>20%）自动告警
- **用户分群分析**：支持按用户属性（新老用户、活跃度等）分群分析

**数据存储要求**：
- **原始数据保留**：至少保留1年的原始行为数据，支持回溯分析
- **聚合数据存储**：按日/周/月聚合统计数据，提升查询效率
- **数据备份**：定期备份统计数据，防止数据丢失
- **隐私保护**：统计数据匿名化处理，符合隐私保护要求

#### 7.5.3 数据驱动迭代机制
**周度数据分析**：
- 每周分析核心行为统计数据，识别异常和趋势
- 重点关注：上传率、推荐点击率、换一件使用率的变化
- 针对异常指标，深入分析原因，制定优化方案

**月度数据报告**：
- 每月生成数据报告，包含：
  - 核心指标完成情况（对比目标值）
  - 用户行为趋势分析
  - 功能使用情况分析
  - 问题识别和优化建议
- 报告同步给产品、技术、运营团队

**数据驱动决策**：
- **功能优化优先级**：基于使用率数据（如换一件使用率高，优先优化换一件功能）
- **算法优化方向**：基于替换品类分布，针对性优化推荐算法
- **用户体验优化**：基于流失点分析（如上传完成率低，优化上传流程）
- **A/B 测试验证**：基于统计数据验证优化效果

---

## 八、开发优先级建议

### Phase 1：MVP 版本（核心功能）
**里程碑时间**：12 周（3 个月）
**交付物**：可运行的 MVP 原型，支持"衣橱上传 + 基础推荐"核心流程

**功能模块与优先级拆分**：

**P0（必须，前 8 周完成）**：
1. **电子衣橱管理 - 上传识别**（优先级最高）
   - 衣物上传功能（单张/批量，最多 9 张）
   - **豆包 AI API 集成**：
     - 豆包 AI API 接口对接（RESTful API）
     - API Key 配置和管理（密钥安全存储）
     - Prompt 提示词设计和优化（确保识别准确率）
     - 批量识别功能实现（批量调用 API）
     - 识别结果解析和映射（将 API 响应转换为内部数据结构）
     - 错误处理与重试机制（网络错误、超时处理、API 限流、服务降级）
     - 识别结果缓存机制（降低 API 调用成本）
   - AI 识别功能（品类、颜色、风格、季节）
   - 识别结果展示和编辑
   - 网络中断处理与断点续传
   - **交付标准**：上传成功率 ≥95%，识别准确率达标，断点续传功能正常，识别速度 < 8 秒（P95），API 调用成功率 ≥98%
2. **AI 自动穿搭推荐 - 基础推荐**
   - 手动推荐功能（选择场合 + 天气）
   - 基础推荐算法（规则引擎为主）
   - 推荐方案展示（3-5 套）
   - **交付标准**：推荐生成成功率 ≥98%，生成耗时 < 3 秒
3. **用户偏好设置**
   - 首次使用引导流程
   - 偏好设置页面（风格、场合、颜色偏好）
   - 偏好数据存储和应用
4. **基础 UI 框架**
   - 核心页面布局（首页、衣橱页、推荐页）
   - 基础组件库（按钮、卡片、输入框等）
   - 导航框架

**P1（重要，第 9-10 周完成）**：
5. **电子衣橱管理 - 筛选管理**
   - 多维度筛选（品类、风格、季节、标签、颜色）
   - **快速筛选"已淘汰"衣物入口**（筛选栏快捷按钮，显示已淘汰数量）
   - 衣橱展示（网格/列表视图）
   - 状态管理（已淘汰、删除）
   - 批量操作（批量恢复、批量删除）
6. **AI 自动穿搭推荐 - 换一件功能**
   - "换一件"交互流程
   - 同品类衣物替换
   - AI 重新评估机制
7. **场合快速选择**
   - 首页快捷按钮（通勤/约会/运动等）
   - 自动获取天气
   - 自定义快捷按钮

**P2（优化，第 11-12 周完成）**：
8. **冷启动方案**
   - 基础搭配模板展示
   - 智能品类补齐引导
   - 虚拟试穿功能入口（埋点）
9. **数据统计基础**
   - 核心行为数据埋点（上传率、推荐点击率、换一件使用率）
   - 基础数据看板（实时监控）
10. **性能优化**
    - 图片压缩和懒加载
    - 推荐结果缓存
    - 页面流畅度优化（FPS ≥ 50）

**验收标准**：
- ✅ 用户可完成完整流程：上传衣物 → 查看识别结果 → 生成推荐 → 查看推荐方案
- ✅ 核心功能可用性 ≥98%
- ✅ 性能指标达标（启动 < 3 秒，识别 < 5 秒，推荐生成 < 3 秒）
- ✅ 页面流畅度 FPS ≥ 50

---

### Phase 2：增强功能
**里程碑时间**：8 周（2 个月）
**交付物**：功能完整的增强版本，支持推送、收藏、记录等增强功能

**功能模块与优先级拆分**：

**P0（必须，前 5 周完成）**：
1. **每日穿搭推送**
   - 推送设置（时间、场景选择）
   - 天气数据集成（位置权限申请）
   - 自动推荐生成
   - 推送通知展示
2. **收藏功能**
   - 推荐方案收藏
   - 收藏管理页面
   - 从收藏中快速应用

**P1（重要，第 6-7 周完成）**：
3. **穿搭记录功能**
   - 穿搭日记（上传照片、标记单品、添加文字）
   - 穿搭回顾（日历视图、搜索、统计）
4. **数据统计完善**
   - 完整的数据埋点体系
   - 趋势分析看板
   - 异常告警机制

**P2（优化，第 8 周完成）**：
5. **离线查看优化**
   - 推荐方案缓存策略优化
   - 离线模式体验优化
6. **性能优化**
   - 推送性能优化
   - 图片加载优化

**验收标准**：
- ✅ 推送功能可用性 ≥95%
- ✅ 收藏功能完整可用
- ✅ 穿搭记录功能完整可用
- ✅ 数据统计看板正常运行

---

### Phase 3：优化与扩展
**里程碑时间**：12 周（3 个月）
**交付物**：功能完善的正式版本，支持分享、离线、日程集成等扩展功能

**功能模块与优先级拆分**：

**P0（必须，前 6 周完成）**：
1. **分享功能**
   - 穿搭卡片生成（精美分享卡片）
   - 分享渠道（微信、微博、小红书、相册）
2. **离线查看完善**
   - 完整离线模式支持
   - 数据同步机制
   - 多设备同步冲突处理

**P1（重要，第 7-9 周完成）**：
3. **日程集成**（可选功能）
   - 日历权限申请（仅读取未来 7 天）
   - 日程关键词识别
   - 根据日程自动推荐
4. **数据导出功能**
   - 数据导出功能实现
   - 导出格式和方式
5. **维护机制建立**
   - 用户行为数据收集和分析系统完善
   - 人工更新流程和工具
   - 算法迭代优化机制
   - A/B 测试框架

**P2（优化，第 10-12 周完成）**：
6. **虚拟试穿功能**（完善 Phase 1 埋点）
   - 外部单品临时加入虚拟衣橱
   - 基于虚拟衣橱生成搭配方案
   - 为购物功能提供数据积累
7. **性能全面优化**
   - 全面性能优化（启动、识别、推荐、页面流畅度）
   - 内存优化
   - 电池优化
8. **视觉和交互优化**
   - 交互动效完善
   - 平板横屏布局优化
   - 折叠屏适配

**验收标准**：
- ✅ 所有功能完整可用
- ✅ 性能指标全面达标
- ✅ 数据统计系统完善
- ✅ 维护机制建立并运行

---

## 九、验收标准

### 9.1 功能验收
- ✅ **品类识别准确率**：
  - 上衣/裤子/裙子/鞋子 ≥95%
  - 配饰 ≥80%
- ✅ **颜色识别准确率** ≥90%
- ✅ **风格识别准确率** ≥85%
- ✅ **季节属性识别准确率** ≥90%
- ✅ **用户修正率** ≤10%（辅助指标，反映真实用户体验）
- ✅ 推荐生成成功率 100%（衣橱有足够衣物时）
- ✅ 支持批量上传（9 张/次）
- ✅ 支持离线查看衣橱和收藏（已识别的数据）
- ✅ 离线状态下不支持上传识别和生成新推荐（需网络连接）
- ✅ **边界场景处理**：
  - ✅ 网络中断时上传照片自动保存到队列，支持断点续传，网络恢复后自动继续上传
  - ✅ 断点续传功能正常：上传中断后可从断点处继续，无需重新上传已传输部分
  - ✅ 网络恢复后自动弹出"正在恢复上传"弹窗，显示恢复进度
  - ✅ 全部衣物标记"已淘汰"时，提示用户并引导恢复部分衣物
  - ✅ 多设备同步冲突时，提供用户选择或自动合并机制

### 9.2 性能验收
- ✅ **启动时间**：
  - ✅ 首次安装启动 < 5 秒（P95）
  - ✅ 常规冷启动 < 3 秒（P95）
  - ✅ 热启动 < 1 秒（P95）
- ✅ **识别速度**：
  - ✅ 单张照片识别 < 8 秒（P95，包含上传和识别时间）
  - ✅ 批量识别（9张）< 30 秒（P95）
- ✅ **推荐生成**：推荐生成 < 3 秒（P95）
- ✅ **页面流畅度**：
  - ✅ 页面滑动 FPS ≥ 50（P95，60Hz 屏幕）
  - ✅ 页面滑动 FPS ≥ 30（P95，低端设备）
  - ✅ **卡顿判定**：页面滑动 FPS < 45 持续 1 秒以上判定为卡顿
  - ✅ **卡顿率**：< 5%（页面滑动过程中出现卡顿的比例）
  - ✅ 列表滚动无卡顿、无掉帧
- ✅ **操作响应延迟**：
  - ✅ 按钮点击响应延迟 < 200ms（从点击到视觉反馈）
  - ✅ 页面跳转动画时长 300ms ± 50ms
  - ✅ 卡片滑动动画时长 300ms ± 50ms
  - ✅ 弹窗出现/消失动画时长 250ms ± 50ms
- ✅ **无崩溃**：崩溃率 < 0.1%（按会话统计）
- ✅ **内存占用**：正常使用状态下内存占用 < 200MB（iOS）/ < 300MB（Android）

### 9.3 用户体验验收
- ✅ 核心流程 3 步内完成
- ✅ 界面美观，符合设计规范
- ✅ 交互流畅，反馈及时
- ✅ 错误提示友好，有解决方案

### 9.4 维护机制验收
- ✅ **数据收集系统**：
  - ✅ 用户行为数据（收藏、应用、不感兴趣）实时收集
  - ✅ 用户修正数据自动回流到训练集
  - ✅ 用户反馈数据（评分、主动反馈）收集机制完善
  - ✅ **核心行为统计指标完整收集**：
    - ✅ 上传相关：上传次数、上传成功率、上传率、批量上传率、上传完成率
    - ✅ 推荐相关：推荐查看次数、推荐收藏率、推荐生成率、推荐点击率、推荐查看完成率、推荐生成耗时
    - ✅ 换一件相关：换一件使用次数、换一件使用率、替换成功率、平均替换次数、替换品类分布
    - ✅ 收藏与应用：推荐收藏率、推荐应用率、收藏后应用率
    - ✅ 偏好设置：偏好设置完成率、偏好设置修改率
    - ✅ 冷启动：冷启动触发率、冷启动模板查看率、冷启动后上传率
    - ✅ 离线使用：离线使用时长、离线功能使用率
  - ✅ **数据统计看板**：实时监控看板、趋势分析看板、异常告警机制已建立
  - ✅ **数据驱动迭代机制**：周度数据分析、月度数据报告、数据驱动决策流程已建立
- ✅ **人工更新机制**：
  - ✅ 搭配规则库每季度更新流程建立
  - ✅ 基础搭配模板更新机制完善
  - ✅ 豆包 AI Prompt 优化流程规范
- ✅ **算法迭代优化**：
  - ✅ 推荐算法基于用户行为数据持续优化
  - ✅ 豆包 AI Prompt 持续优化机制建立
  - ✅ A/B 测试框架支持算法效果验证
- ✅ **效果评估指标**：
  - ✅ 推荐方案收藏率 ≥30%
  - ✅ 推荐方案应用率 ≥20%
  - ✅ 用户满意度评分 ≥4.0/5.0
  - ✅ 用户修正率 ≤10%

---

## 十、后续迭代方向（可选）

### 10.1 社交功能
- 用户可分享穿搭到社区
- 关注其他用户，查看他们的穿搭
- 点赞、评论互动

### 10.2 购物功能
- 识别衣物品牌，推荐相似单品购买链接
- 与电商平台合作，推荐搭配单品

### 10.3 高级功能
- 支持 3D 试衣（AR 技术）
- 支持穿搭视频录制和分享
- 支持 AI 生成穿搭灵感图（基于用户衣橱）

---

**文档版本**：v1.0  
**创建日期**：2024  
**最后更新**：2024
