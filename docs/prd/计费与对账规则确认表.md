# 计费与对账规则确认表

> 用途：拉**财务 + 技术负责人**过一遍，逐条把"未知"定死。这是 PRD 里**唯一阻塞资金/统计开发**的前置。
> 用法：「PM 提案」是我基于行业惯例给的默认值，方便你们**改**而不是从零写；请在「确认/修正」列填最终结论；不确定的标 ⚠️ 转 Phase 2。
> 定稿后回填进 PRD《全局业务约定》与日终/账户流水的口径说明。

最后更新：2026-07-15
状态：⬜ 待对齐

---

## A. 费率与利润（最高优先级，阻塞日终「利润」列）

设订单金额 = M。

| # | 规则项 | 待确认问题 | PM 提案（行业惯例） | 确认/修正 |
|---|-------|-----------|-------------------|----------|
| A1 | 商户服务费 | 计算方式？基数？ | M × 商户费率（小数，0.003=3‰） | |
| A2 | 通道成本 | 平台付通道的费用？基数？ | M × 通道费率（三档，按金额区间） | |
| A3 | 代理费 | 基数是**订单金额**还是**商户服务费**？ | ⚠️ 二选一：① M × 代理费率；② 商户服务费 × 代理费率 | |
| A4 | 平台利润公式 | 利润 = ？ | 商户服务费 − 通道成本 − 代理费 | |
| A5 | 利润是否含补单/回调 | 历史回调/补单、当日补单、测试补单金额是否进利润？ | ⚠️ 建议：仅"当日补单"计入，测试补单不计；待确认 | |
| A6 | 各日终「利润/利润率」口径 | 分子分母？ | 利润 = 同 A4；利润率 = 利润 / 成交额 | |

---

## B. 入账规则（阻塞账户流水与「入账状态」）

| # | 规则项 | 待确认问题 | PM 提案 | 确认/修正 |
|---|-------|-----------|--------|----------|
| B1 | 入账时点 | 支付成功即入账，还是结算才入账？ | 支付成功 → 商户余额入账（贷）；服务费同时扣 | |
| B2 | 入账状态三态 | 未支付 / 已入账 / 未入账 的确切边界 | 未支付=未成交；已入账=成功且入商户余额；未入账=成功但未落账（异常） | |
| B3 | 服务费扣除时点 | 支付时扣 or 结算时扣？ | 支付成功时扣商户服务费；通道成本日终结算 | |
| B4 | 回调失败的资金 | 回调失败但已支付，钱在哪？ | 已入商户余额，回调状态独立标记；是否冲销待定 | |

---

## C. 结算规则（阻塞「余额清零 / 结算昨日」与自动结算）

| # | 规则项 | 待确认问题 | PM 提案 | 确认/修正 |
|---|-------|-----------|--------|----------|
| C1 | 余额清零 | 结算"累计跑量"指什么？ | 把商户累计待结算余额一次性结出/清零到结算账户 | |
| C2 | 结算昨日 | "仅昨日跑量"指什么？ | 只结算昨日成交产生的待结算额 | |
| C3 | 自动结算触发 | 定时 Cron 按配置时间执行？ | 是，按商户/供应商各自配置的每日时间 + 结算方式 | |
| C4 | 结算动哪个科目 | 结算后钱从哪→到哪？ | 商户可用余额 → 结算/提现池（具体科目待财务定义） | |

---

## D. 提现规则（阻塞提现审核与资金流转）

| # | 规则项 | 待确认问题 | PM 提案 | 确认/修正 |
|---|-------|-----------|--------|----------|
| D1 | 提现服务费 | 固定/笔 还是 比例？ | 固定金额/笔（截图见 0.50、2.00） | |
| D2 | 提现扣哪余额 | 从商户"可用余额"扣？是否冻结？ | 申请时冻结，审核通过实际出账；拒绝解冻 | |
| D3 | 审核通过资金流 | 通过后钱去哪？记账？ | 可用余额 → 提现中 → 已到账；写账户流水 | |
| D4 | 拒绝必填备注 | 已定（拒绝必填） | ✅ 已确认 | ✅ |

---

## E. 调额 / 预付 / 信用额度（阻塞余额科目设计）

| # | 规则项 | 待确认问题 | PM 提案 | 确认/修正 |
|---|-------|-----------|--------|----------|
| E1 | 余额科目 | 商户有「余额」+「预付款」两栏，分别是什么？ | 余额=可提现；预付款=平台垫资/授信，可为负 | |
| E2 | 调额 | 调额改的是余额还是预付？方向？ | 调额=人工增/减某科目，必填备注 + 操作日志 | |
| E3 | 信用额度 | 通道/供应商的「信用额度」+ 开关 含义？ | 授信上限，开关=是否启用授信消费 | |
| E4 | 实时预付余额为负 | 供应商预付可负表示什么？ | 已消费超过预存，触发「预付不足提醒」 | |
| E5 | 应下发金额 | 供应商「应下发金额」口径？ | 待付给供应商的累计金额 | |

---

## F. 账户流水与对账（阻塞流水表结构与导出）

| # | 规则项 | 待确认问题 | PM 提案 | 确认/修正 |
|---|-------|-----------|--------|----------|
| F1 | 新旧余额成对 | 每笔流水必须记 旧余额/新余额？ | ✅ 是，便于对账（PRD 已约定） | ✅ |
| F2 | 流水「类型」枚举 | 充值/提现/结算/调额/回调/手续费/退款… 全集？ | ⚠️ 待补图展开下拉（暂用：支付入账/服务费/提现/结算/调额/退款/补偿） | |
| F3 | 日切 | 日终按 UTC+8 00:00 聚合？ | ✅ 已确认 | ✅ |
| F4 | 对账主体 | 5 类账户（商户/通道/代理/系统/供应商）余额是否两两独立、互不串账？ | ✅ 独立账户体系 | ✅ |

---

## 对齐结论（会议产出）

- 公式最终版（A1–A6）：__________
- 入账/结算最终口径（B/C）：__________
- 提现/调额/科目最终定义（D/E）：__________
- 转入 Phase 2 的项：__________
- 责任人 & 回填 PRD 日期：__________

> ⚠️ 关键依赖：本表 A/B 两节不定，**Phase 1 的账户流水、商户提现、日终基础报表都无法验收**。请优先对齐 A、B。
